# iOS Contract

См. основной контракт: `docs/CONTRACT.md`.

Интеграция:
- Вызываем: /api/check, /api/reverse-geocode, /api/whereami.
- Кешируем на клиенте: последний country/region + timestamp обновления.
