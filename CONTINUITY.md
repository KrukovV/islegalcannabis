Goal: Keep one canonical `/new-map` MapLibre runtime across `/`, `/new-map`, and `/c/[code]`, with CountryPageData as the only SEO/map/card truth, stable nearby/social-reality enrichment, exact non-regressing map visuals, truthful local overlays, and a live local AI companion that stays honest about model quality.
State: checkpoint=.checkpoints/20260529-135125.patch; CI=PASS; Smoke=PASS
Done: Route/map/SEO truth remains locked on the unified `/new-map` stack. The AI companion remains single-model on `qwen2.5:1.5b` with no runtime fallback switching. Context is lightweight v2 with short history, location anchoring, deterministic retry/final guards, and deterministic engines for nearby plus fallback truth blocks. Nearby AI integration formats truth-engine output as 4 ranked places with effective km, access type, destination `Risk:`, and the mandatory border warning. Slang/culture/chat/legal now stay in one LLM-first companion loop: router hints enrich prompt, follow-up intents carry into context, geo is disabled for slang/chat, CPU false-stop for Ollama is removed, intent inheritance now upgrades `general` into carried `legal/culture` where needed, and UI now prefers the validated final answer over concatenated retry stream text. Deterministic fallback stays only after failed validation. GEO/safety metadata is no longer displayed in the UI unless a real safety note is attached. Canonical production host is now `https://www.islegal.info`: metadataBase, Open Graph URLs, sitemap builders, both `robots.txt`, middleware, and host redirect now point to `www`, with non-www redirecting to `https://www.islegal.info`. Targeted metadata/sitemap tests pass, lint is green, and local production build passes. Deployment commit `c25f151` is pushed to `origin/main` with tags `good/20260430-125146` and `prod/20260430-www-canonical`. Source-quote sanitation is fixed in both the visible SEO page block and the serialized `seoCountryData` client payload; deployment commits `86e7f23` and `e6c08d2` are pushed to `origin/main`, and live HTML checks confirm `www` sitemap URLs plus clean Wyoming source quotes without the leaked wiki `style=\"background:#C4C9CD;\" |` preamble. False `TAG_NOT_PUSHED` is fixed in `tools/commit_if_green.sh`: the old `git ls-remote | grep -q` check was broken by `pipefail`/SIGPIPE, and the new targeted tag-ref helper now publishes commit `a1b068b` with `HEAD_PUSHED=1`, `TAG_PUSHED=1 name=good/20260430-132759`, and `TAG_PUSHED=1 name=prod/20260430-tag-verification-fix`. Latest deploy commit `33ee395` is pushed to `origin/main` with tags `good/20260430-134227` and `prod/20260430-ssot-ledger-sync`. Bing Webmaster verification meta tag `msvalidate.01` was added to the shared root layout head. The `apps/web npm run dev` entry now uses the singleton-safe `tools/ui/ui_dev_ssot.sh` wrapper, so it exits cleanly with `UI_ALREADY_RUNNING` when port 3000 is occupied instead of crashing on `EADDRINUSE`. Sitemap multi-delivery is live on `www` with five delivery URLs, and middleware now forces `Cache-Control: no-store` on sitemap paths while robots no longer blocks `/api/sitemap` or query-based sitemap delivery. Final live validation on 2026-05-04 confirms full edge propagation: `https://islegal.info/sitemap.xml` performs exactly one redirect to `https://www.islegal.info/sitemap.xml`, `sitemap.xml`, `sitemap-index.xml`, and `api/sitemap` now return `200` with XML content and `Cache-Control: no-store`, and `robots.txt` exposes all sitemap URLs without `Disallow: /api/` or `Disallow: /*?*`.
Now: Turbopack broad-pattern data path warnings fixed locally by replacing dynamic repo-root scans with explicit server-side data path allowlists; lint and build are green, broad-pattern warnings are absent. Local server-side IP geolocation now resolves through `/api/geo/loc` without browser calls to `ipapi.co` or `ipwho.is`; localhost smoke returns `iso=DE`, `geo=DE-HESSE`, `provider=ipinfo`, and country display is derived generically from ISO via `Intl.DisplayNames`. Cold-start fix is published to `origin/main` as commits `7952c8a` and `eab9fe8` with green tag `good/20260526-211608`: runtime countries URL is now content-hashed `/static/countries/countries.2ef3c2e23260.json`, `/api/new-map/countries` redirects to it, WebKit cold-start e2e/docs/spec are updated, `npm -w apps/web run lint` plus `npm -w apps/web run build` are green, focused WebKit cold-start proof is green, `PLAYWRIGHT_BASE_URL=http://127.0.0.1:61346 npm -w apps/web run ui:e2e` is green with 11 passed / 1 skipped, and `bash tools/pass_cycle.sh` is green with `CI_STATUS=PASS`, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, `PASS_CYCLE_EXIT rc=0`. Local web/mobile runtime viewport pass on fresh production-local `61346` is green: 9-device Playwright matrix (desktop Chrome/WebKit, iPhone SE, iPhone 15 Pro Max, Pixel 8, Galaxy Fold Closed, iPad Mini, iPad Pro, landscape) has `viewport-fit=cover`, one MapLibre canvas, no horizontal overflow, no supplemental Antarctica layer/source, stable AQ palette `#c5ccd3/#d4dae0`, popup open, route panel visible, and screenshots in `QA/web-local-runtime/post-change/`; local `PLAYWRIGHT_BASE_URL=http://127.0.0.1:61346 npm -w apps/web run ui:e2e` is green with 11 passed / 1 skipped. Production after deploy now serves `/static/countries/countries.2ef3c2e23260.json` as `200` with `Cache-Control: public, max-age=31536000, immutable`, `/api/new-map/countries` returns `308`, and Vercel cache reaches `HIT` on WebKit. Before deploy prod used `/api/new-map/countries`, `12.46 MB decoded`, `Cache-Control: public, max-age=0`, cache `MISS`; after deploy prod uses `1.51 MB transfer / 4.08 MB decoded`, WebKit countries response `858 ms`, full first fill `2.75 s` (`2.10 s` from route-start), Chromium countries response `4.41 s` on a MISS, full first fill `6.43 s` (`5.44 s` from route-start). Unified map mobile/web runtime hardening is published as commit `0533eaf` with tags `good/20260527-133532` and `prod/20260527-web-mobile-runtime`. Prod post-deploy clean matrix is green on 9 devices with Vercel bypass cookie: desktop Chrome/WebKit, iPhone SE, iPhone 15 Pro Max, Pixel 8, Galaxy Fold Closed, iPad Mini, iPad Pro, and landscape all have viewport-fit cover, no horizontal overflow, viewport-matching map surface/canvas, one MapLibre canvas, popup open, route panel visible, stable layers, no supplemental Antarctica layer/source, and static countries cache HIT. Stable rollback base is published to `origin/main` as commits `9b680cc` and `9feb99f` with tag `stable/20260529-production-map-runtime`. PageSpeed/Treemap local micro-optimization is in progress: card-index was removed from head prefetch and Yandex Metrika was moved to `lazyOnload`; local lint/build are green, focused WebKit cold-start/preload is green, UI e2e is green with 11 passed / 1 skipped, and repeated production-local timing shows median `NM_T7_FIRST_FILL_RENDERED` improved from Chromium `6715ms` to `2150ms` and WebKit `900ms` to `809ms` without horizontal overflow or canvas-count changes. A tiles preconnect candidate was tested and rejected locally because median first-fill did not improve (`Chromium 2150ms -> 2216ms`, `WebKit 809ms -> 955ms`); the change was rolled back before prod. A lazy card-index-on-tap candidate was also tested and rejected locally because it removed the startup card-index request but did not improve first-fill (`Chromium 2150ms -> 2242ms`, `WebKit 809ms -> 815ms`) and local Lighthouse regressed; the product files were restored before prod. A source-URL countries candidate is locally green, full `bash tools/pass_cycle.sh` green, and not deployed: it lets MapLibre load the same immutable countries JSON directly instead of parsing it on the main client path; duplicate `<link preload>` was rejected and removed. Final production-local proof after rebuild/restart shows single countries request per run, no browser `ipapi.co`/`ipwho.is`, one canvas, overflow `0`, rendered countries `235`, `/c/can` panel visible, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:61346 npm -w apps/web run ui:e2e` green with 11 passed / 1 skipped. Local medians are Chromium T7=`2059ms` and WebKit T7=`688ms`; local Lighthouse desktop is Performance `71`, FCP `0.3s`, LCP `1.8s`, TBT `440ms`, CLS `0.002`. Post-rollback startup proof remains stable: local Chromium/WebKit T7=`3409ms`/`927ms`, prod Chromium/WebKit T7=`4382ms`/`793ms`, prod static countries is `1.51 MB transfer / 4.08 MB decoded` with immutable cache and HIT, and browser-side `ipapi.co`/`ipwho.is` calls are absent in the measured runs. Follow-up prod smoke retries hit Vercel Security Checkpoint `Code 21` after repeated automation despite the bypass token, so additional prod Playwright retries are stopped to avoid rate-limit amplification. Direct Lighthouse CLI against prod remains `CHROME_INTERSTITIAL_ERROR` even with bypass query, bypass header-file, cookie flag, persistent storage, and normal Chrome UA, while HTTP/Playwright with the same bypass reaches the real app; prod Lighthouse evidence therefore uses Playwright/PageSpeed UI until Vercel protection accepts Lighthouse navigation. Headed-browser right white strip is confirmed as test-window chrome outside the emulated viewport, not product overflow: saved Pixel 8 viewport PNG is 1082x2402 and DOM reports inner/outer/root/surface/canvas width 412 with `horizontalOverflowPx=0`. Real-device smoke probe on this host is `UNCONFIRMED`: `xcrun xctrace list devices` exposes only the MacBook plus simulators, no physical iPhone/iPad, and `adb` is unavailable, so physical iPhone Safari and Android Chrome smoke cannot be honestly marked passed from this machine. Probe artifact is `QA/mobile/real-device-smoke/2026-05-28-device-probe.json`. Latest checkpoint `bash tools/pass_cycle.sh` is green with `CI_STATUS=PASS`, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, and `PASS_CYCLE_EXIT rc=0`; pass_cycle also performed normal SSOT snapshot/cache rotation. Evidence screenshots are stored in `QA/mobile/cold-start/prod-before-deploy/`, `QA/mobile/cold-start/prod-after-deploy/`, `QA/web-prod-runtime/post-deploy-clean/`, and `QA/pagespeed/20260529-source-url-candidate/`.
Open questions: UNCONFIRMED `deepseek-r1:1.5b` remains installed but is still rejected for runtime promotion on this host, and `deepseek-r1:7b` remains intentionally uninstalled on `8 GB RAM`. Other locally installed models may still exist in Ollama tags for diagnostics, but the product runtime default is intentionally single-model.
