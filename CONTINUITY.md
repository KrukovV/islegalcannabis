Goal: Document the current stable Vercel bypass production QA stack, then create and push a stability commit/tag through the approved green-commit path.
State: checkpoint=.checkpoints/20260614-224737.patch; CI=PASS; Smoke=PASS
Done: CI/live-gate patch routes production audit tail to `https://www.islegal.info/new-map?qa=1`, uses shared `warmVercelBypass` in `tools/vercel_bypass_live_probe.mjs`, skips payload/js/gps after live-gate fail, skips production tail after local pipeline fail, and makes extended payload/js/gps gates opt-in with `PROD_EXTENDED_TAIL_GATES=1`. Targeted checks PASS: `node --test tools/vercel_bypass.test.mjs`, `node --test tools/prod_live_quality_gate.test.mjs`, `node --test tools/ocean_background_pixel_analyzer.test.mjs`, `node --check tools/prod_zoom_ocean_repeatability.mjs`, `node --check tools/vercel_bypass_live_probe.mjs`, `bash -n tools/pass_cycle.sh`, `node tools/gates/log_size_guard.mjs`, and `git diff --check`. Full production zoom/ocean matrix `Artifacts/prod-repeatability/20260614T154609/prod_zoom_ocean_repeatability.json` PASS: 3 runs x Chromium+WebKit x 15 territories x 5 cycles, `sessions=6`, `rows=900`, `fail_rows=0`, `challenge_count=0`, `blank_tile_rect_count=0`, `max_idle_ms=4579`, `avg_idle_ms=606`, `largest_near_white_component_px=84`, `placeholder_gray_ratio=0.002477`, `screenshots=1890`, `SECRET_LEAK_GUARD=PASS`; screenshots archived outside repo at `/Users/james/islegalcannabis_archive/artifacts/prod-repeatability/20260614T154609/screenshots` and the JSON report points to that archive. Repeat production zoom/ocean matrix `Artifacts/prod-repeatability/20260614T172737/prod_zoom_ocean_repeatability.json` PASS: `sessions=6`, `rows=900`, `fail_rows=0`, `challenge_count=0`, `blank_tile_rect_count=0`, `max_idle_ms=4928`, `avg_idle_ms=1576`, `largest_near_white_component_px=84`, `placeholder_gray_ratio=0.002477`, `screenshots=1890`, `SECRET_LEAK_GUARD=PASS`; screenshots archived outside repo at `/Users/james/islegalcannabis_archive/artifacts/prod-repeatability/20260614T172737/screenshots`, and `Reports/ProdAudit/zoom-ocean-repeatability.md` records both green matrix runs and the latency delta. `LOG_SIZE_GUARD=PASS` after archiving heavy screenshots and aborted run artifacts. `pass_cycle` exposed live-probe timeout mismatch: baseline allowed `max_map_ready_ms=60000`, but probe waited only about 25s and failed with `NO_MAP_READY`; `tools/vercel_bypass_live_probe.mjs` now defaults `VERCEL_BYPASS_APP_READY_TIMEOUT_MS` to 65000 and applies it to root/surface/map-ready/canvas waits. Local tests after the fix PASS, one controlled production live gate PASS (`map_ready_ms=8842`), and final `bash tools/pass_cycle.sh` PASS: `CI_STATUS=PASS`, `PIPELINE_RC=0`, `PROD_LIVE_OK=1`, `PROD_EXTENDED_TAIL_SKIPPED=1 reason=PROD_BUDGET_DEFAULT`, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, `PASS_CYCLE_EXIT rc=0`. Documentation/spec updated: `docs/VERCEL_BYPASS.md` is the new bypass SSOT; `README.md`, `docs/OPS.md`, `docs/DEV.md`, `docs/REPO_POLICY.md`, `docs/CONTRACT.md`, and `docs/PLAN.md` now describe context-request cookie warmup, `/new-map?qa=1`, stop-on-challenge, secret redaction, extended tail opt-in, and stability-tag requirements. Post-doc targeted checks PASS: `node --test tools/vercel_bypass.test.mjs`, `node --test tools/prod_live_quality_gate.test.mjs`, `node --test tools/ocean_background_pixel_analyzer.test.mjs`, `node tools/gates/log_size_guard.mjs`, and `git diff --check`.
Now: Running the required full `bash tools/pass_cycle.sh` before commit/tag/push.
Open questions: Extended production payload/js/gps gates are UNCONFIRMED in this default pass because they are intentionally opt-in to avoid burning Vercel attempts after live proof.
