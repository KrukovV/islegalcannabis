Goal: Fix `/new-map` country-label latency and stale GPS refresh behavior locally and on production, with measured before/after deltas that do not regress current PageSpeed/Lighthouse budgets.
State: checkpoint=.checkpoints/20260615-225521.patch; CI=PASS; Smoke=PASS; current diff preserves multilingual labels and map behavior while adding GPS refresh recovery plus basemap warmup; production is still on the rollback commit until the new publish completes
Done: Extracted `1.mov` into 154 frames at 0.5s intervals under `Artifacts/video_frames/` and confirmed the user-visible delay window; fixed `apps/web/src/new-map/MapGeoDock.tsx` so repeated GPS clicks recenter immediately and still request a fresh browser geolocation; kept label content/logic intact while speeding the basemap path by prefetching `/api/new-map/basemap-style`, sharing runtime URLs, and inlining tilejson into the proxied style source; updated QA Playwright routes to use `?qa=1`; targeted specs `new-map.geo`, `new-map.preload`, and `new-map.geo-perf` passed; `npm -w apps/web run build` passed; local measured deltas improved: GPS `FAIL -> PASS`, payload `first_fill_ms 3627 -> 2429`, long-task total `4849 -> 4064`; archived oversized generated screenshots and split frame dumps to `~/islegalcannabis_archive/20260615-new-map-perf` so storage guards pass again
Now: Commit and push the current safe patch, wait for the new production deployment, and rerun live prod JS/payload/GPS measurements through the approved bypass query.
Open questions: UNCONFIRMED whether the production deployment fully clears the current live label timeout and map-ready timeout without any further runtime tuning.
