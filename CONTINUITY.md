Goal: Eliminate cross-surface GEO desync for all 307 runtime entities so one canonical wiki-derived knowledge/legal/color model drives map color, popup compact view, and SEO extended view without same-name collisions or country-specific patches.
State: checkpoint=.checkpoints/20260629-212410.patch; CI=PASS; Smoke=PASS
Done: Fresh full popup/wiki visual audit was rerun after the latest source/script changes and now passes `node tools/gates/popup_visual_audit_guard.mjs`: `total_geo_count=307`, `processed_geo_count=307`, `popupCaptured=307`, `wikiCaptured=307`, `enriched=295`, `honestly_sparse=30`, `no_page=8`, `regressions=0`, `ui_compaction_fixed=25/25`, `conflicted=0`. Repo keeps lightweight `Artifacts/popup-visual-audit/full-*` reports; heavy popup/wiki screenshots and per-GEO wiki/json/html payload were archived to `/Users/james/islegalcannabis_archive/20260628-popup-audit-refresh-1931/`, and manifest paths were rewritten to existing absolute archive paths. Fresh full geo-sync was rerun after `apps/web/scripts/geo-sync-audit.ts` changes and now has `total=307`, `processed=307`, `mapCaptured=307`, `popupCaptured=307`, `seoCaptured=307`, `wikiCaptured=307`, `missing_artifacts=[]`, `regressions=[]`, `source_trace_errors=0`, `color_mismatch=0`, `status_conflicts=0`, `seo_not_richer=0`, `low_coverage=53`, `no_page_or_synthetic=10`. Heavy per-GEO map/popup/SEO/wiki screenshot/json payload was archived to `/Users/james/islegalcannabis_archive/20260628-geo-sync-full-2105/geo-sync`, and `Artifacts/geo-sync/full-*` manifests/reports/index now point to existing archive paths. `node tools/gates/log_size_guard.mjs` passes with `artifacts_bytes=20252429`. Earlier generic resolver cleanup for ambiguous cannabis titles such as `Cannabis in Georgia` is still documented, but popup/wiki evidence is no longer treated as sufficient release proof for resolver/color/SEO correctness. Project docs/spec are now refreshed again: `README.md`, `docs/CONTRACT.md`, `docs/DEV.md`, and `docs/PLAN.md` now state that popup/wiki `307/307` alone does not close resolver/color sync work, and new `docs/GEO_SYNC_AUDIT.md` defines the required full `307` GEO map/popup/SEO/wiki/color contract, anti-patch rules, per-GEO artifact bundle, canonical identity requirements, hard fail gates, and reporting expectations. The contract now explicitly requires screenshot-based comparison in two planes: inside the project (`map ↔ popup ↔ SEO`) and project against Wiki (`popup/SEO/color outcome ↔ wiki article`). `UnifiedSeoStatusPanel` now renders the same canonical cannabis-profile section model as popup/SEO pages and also supports runtime fallback entries that do not have their own `CountryPageData` file. `/new-map?geo=...` now forwards query geo/code into `initialGeoCode`, so runtime-only territories/synthetic GEO can open a geo-specific SEO overlay instead of silently falling back to nothing. `apps/web/scripts/geo-sync-audit.ts` now captures expanded scrollable panel screenshots for popup/SEO, writes `full-*` artifacts only on a real full run, treats missing `CountryPageData` for runtime fallback GEO as a coverage note rather than a source-trace error when wiki/source_kind exist, and marks law/source-only fallback/root cases with explicit low-coverage reasons. The full geo-sync evidence proves `GE` vs `US-GA` stay separated with distinct Wiki sources/content, and former `seo=0` fallback GEO such as `BJN,BQ,BV,CX,XK,TK` now all capture `seo=1`.
Now: Production first-click popup latency was measured against historical `Reports/Perf/prod-performance.json`: historical `CLICK_TO_POPUP_MS` p50=64 ms/p90=99 ms, pre-fix prod FR warm first-click p50=1906 ms/p90=2451 ms/max=4267 ms. Root cause was first click waiting on `/api/new-map/card-entry`/card-index data before rendering the popup. Fixes: `.vercelignore` keeps deploy context near 20MB instead of 1.1-1.4GB while preserving product data; `apps/web/src/app/api/new-map/card-index/route.ts` uses module cache; `/api/new-map/card-entry` fetches only the clicked GEO card; `MapRoot` now renders an immediate honest seed popup from the already-rendered map feature and replaces it with full `card-entry` data when the request returns, so network latency is no longer on the first-paint path. `MapRoot` no longer statically imports `countryCardEntry -> cannabisProfile -> knowledge_db.json` into the initial `/new-map` client bundle; that SEO/profile conversion is lazy-loaded only when `CountryPageData` overlay data is present. New local predeploy budget `node tools/new_map_local_js_budget.mjs` measures `.next` route JS before Vercel upload and currently passes with `route_gzip_kib=454.3` against `local_max_gzip_kib=675`. Latest production `dpl_FEXfr6SwekoqyZaDqTEue1tKfSXX` is READY and aliased to `https://www.islegal.info`; upload used `--archive=tgz`, transferred 19.5MB, and extracted 2610 deployment files. `https://www.islegal.info/api/build-meta` returns build `K8tj4mpJOEkkPMt4vNC-V`, commit `49d3beb13218c0c451a4789ea49a3f6430467e7c`, builtAt `2026-06-29T18:01:21.367Z`. Final `bash tools/pass_cycle.sh` passes: `CI_STATUS=PASS`, `SMOKE_STATUS=PASS`, `SMOKE_PASSED=16`, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, process-slot runtime guard PASS. Prod JS/city gate passes: `PROD_JS_CITY_OK=1`, `first_party_script_kib=432.6`, country labels visible in 1334 ms, city labels visible in 532 ms. Real `mouse` first-click prod gate passes on `FR,GE,US-GA`: `PROD_FIRST_CLICK_OK=1`, wall p50=71 ms/p90=119 ms/max=119 ms, trace p50=2 ms/p90=2 ms/max=2 ms, with visible popup screenshots written under `Reports/new-map-first-click/`. Prod payload and GPS gates pass; GPS hover remains pointer and zoom label checks pass. `tools/prod_new_map_first_click_gate.mjs` now owns a project process slot and screenshots the actually visible popup element with a page-clip fallback, so replaced/seed DOM nodes cannot create false zero-byte screenshot failures. Heavy regenerated `Artifacts/geo-sync/<GEO>` and old prod-gate screenshot folders were moved to `/Users/james/islegalcannabis_archive/20260629-prod-latency-and-geo-sync-heavy/`; additional accumulated prod-gate Reports were archived to `/Users/james/islegalcannabis_archive/20260629-prod-gate-heavy-reports-212242/`. `Artifacts/geo-sync/full-manifest.json` paths were rewritten to the archive and verified with `rows=307`, `checked=2450`, `missing=0`. `node tools/gates/log_size_guard.mjs` passes with repo `Reports`/`Artifacts` under guard. `tools/commit_if_green.sh` and `Tools/commit_if_green.sh` now stage existing release paths including `.vercelignore`, `apps`, `docs`, `packages`, `tools`, `Tools`, `data/wiki`, `data/ssot_snapshots`, `data/ssot_diffs.json`, `data/official`, `cache/ssot_diff_cache.json`, and package files so new app/docs/deploy files are not missed before push/tag.
Open questions: UNCONFIRMED
