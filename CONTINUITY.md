Goal: Keep one canonical `/new-map` MapLibre runtime across `/`, `/new-map`, and `/c/[code]`, preserving map geometry, palette, popups, geolocation, SEO, mobile/runtime QA, and production performance evidence.
State: checkpoint=.checkpoints/20260530-122843.patch; CI=PASS; Smoke=PASS
Done: Unified map/mobile runtime, static hashed countries payload, server-side geo proxy, and production mobile/web QA gates are published. Current stable production map runtime is `6485d35` with tags `good/20260530-webvisor-interaction` and `prod/20260530-webvisor-interaction`. Production serves `/static/countries/countries.2ef3c2e23260.json` with immutable cache, keeps one MapLibre canvas, keeps AI input closed on prod, and has no supplemental Antarctica layer/source. Physical iPhone/Android smoke remains UNCONFIRMED on this host because no real device/adb is available.
Now: Webvisor remains enabled but is deferred until explicit user interaction or late idle fallback. Local and prod Playwright proof show no Yandex requests before `NM_T7` or after 35s passive wait, Yandex loads after interaction, browser IP providers are not called, `/c/can` route panel opens, `ym-disable-keys` is present on the AI input, overflow is `0`, and canvas count is `1`; screenshots are in `QA/pagespeed/20260530-webvisor-interaction/{local,prod}/`. Fresh PageSpeed UI root desktop report `8v69ulyqp1` shows Webvisor/browser console error removed, FCP `0.2s`, LCP `2.4s`, TBT `1540ms`, CLS `0.002`, Speed Index `2.5s`, and performance score `52`; previous user report `ycckumux3n` was score `53`, FCP `0.2s`, LCP `2.3s`, TBT `1460ms`, CLS `0.002`, Speed Index `2.4s`, with browser errors present. Vercel bypass runbook is documented in `docs/OPS.md`: first navigation seeds `x-vercel-protection-bypass` plus `x-vercel-set-bypass-cookie=samesitenone`, follow-up header is scoped to `https://www.islegal.info/**` only, and headed Playwright is required when headless browser verification still lands on Vercel checkpoint. Scoped headed prod proof is green for Chromium desktop and WebKit mobile with commit `6485d35`; screenshots and summary are in `QA/pagespeed/20260530-bypass-docs-deploy/pre-prod-headed-scoped/`.
Open questions: UNCONFIRMED Remaining performance bottleneck is main-thread/MapLibre/Next JS work, not Webvisor. Further score work requires a separate guarded payload/runtime optimization with before/after screenshots and rollback if map geometry, palette, popup, geolocation, AI lock, or startup timings regress.
