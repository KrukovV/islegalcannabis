Goal: Keep runtime and popup behavior stable on local/prod-equivalent (`stability/0.0.10` baseline), keep a full-dataset map route functional, and continue advancing full wiki-audit coverage fixes.
State: checkpoint=.checkpoints/20260622-231310.patch; CI=PASS; Smoke=PASS
Done: Fixed `MapRoot.tsx` debug/bootstrap behavior and status panel labels for route overlays: `handleOpenDetails` uses `const`, SEO panel level titles are readable in English (`Illegal` / `Restricted` / `Legal or partly allowed`), and `__NEW_MAP_DEBUG__` now exposes map in tests for startup/state checks while preserving `data-category` color tokens. Updated `countryIntentContent` test assertion to be semantic (not brittle raw word choice). Popup-profile invariants now hold across the full 300-entity dataset: jurisdictions without a dedicated `Cannabis_in_*` page do not synthesize History/Culture/Products/etc. from root-summary notes, raw URLs are stripped from rendered evidence text, and repeated profile sentences are deduped across rendered sections. Added canonical 300-entity harvester scope from `data/wiki/wiki_claims.json`, local cached-cannabis article discovery, audit-driven unresolved dedicated-page batching with checkpoint/progress output (`Artifacts/popup-profile-harvest-checkpoint.json`, `Reports/knowledge-harvester/progress.json`), redirect-following MediaWiki page resolution, and search fallback for dedicated cannabis titles. Audit truth now treats `sourceType=missing_wikipedia_article` as authoritative even if legacy `wikiUrl` is present, and new missing-page harvest records no longer persist synthetic dedicated-page URLs. The full-run wrapper now audits before harvest, derives the live dataset size instead of hardcoding `300`, defaults to fetch-capable mode unless `KNOWLEDGE_HARVEST_CACHE_ONLY=1` is explicitly set, and writes `cache_only`, `worklist_total`, `batch_total`, and `status_review_overrides` into popup full-run artifacts. Popup cards now always include the exact dedicated `Cannabis_in_*` article in visible popup sources whenever profile sections are rendered, even if official citations are also present, and the audit now fails with `VISIBLE_PROFILE_WITHOUT_LINKED_WIKI_SOURCE` if that canonical profile page link disappears. The knowledge harvester and runtime card pipeline now persist `cultivation` and `market` sections instead of dropping them on write/read; regenerated `data/cannabis_profiles/knowledge_db.json` now has `89` entities with cultivation facts and `79` with market facts, and `Reports/knowledge-harvester/first_wave_validation.{json,md}` now publish `cultivationCount` and `marketCount` per entity. Popup profile sections now carry their canonical dedicated-article source in the model and render that linked label directly in each visible section heading; the audit now fails with `VISIBLE_PROFILE_SECTION_WITHOUT_MATCHING_SECTION_SOURCE` if section content is shown without that matching section-level source. Fixed `docs/DEV.md` path drift, restored `npm run where` -> `bash tools/where-am-i.sh`, updated the workspace guard to `/Users/james/Projects/isLegal`, and documented `ALLOW_SCOPE_OVERRIDE=1` for intentionally wide worktrees. Added popup garbage-text cleanup and gating across the harvester, renderer sanitizer, and audit: cache-backed full-run initially exposed `processed=297/300` with `garbage_text=3`, then repeated 300-entity full-runs rewrote the dataset back to `processed=300/300` with `garbage_text=0`, `raw_urls=0`, `repeated_text=0`, `source_errors=0`, `POPUP_PROFILE_AUDIT_OK=1`, and visible `POPUP_PROFILE_STATUS_REVIEW_OVERRIDES=23`. Validation PASS: `node --test tools/knowledge/harvest_cannabis_knowledge.test.mjs`, `npm -w apps/web exec -- vitest run src/app/_components/CountrySeoPage.test.ts src/new-map/components/viewport-country-popup-render.test.ts`, repeated `KNOWLEDGE_HARVEST_CACHE_ONLY=1 npm run popup:profile:full-run`, and final `bash tools/pass_cycle.sh`. Generic status-derive cleanup now removes six manual status overrides (`AL`, `US-UT`, `US-NE`, `US-KS`, `US-WI`, `US-WY`) from `data/status-engine/manual_review_overrides.json` without per-geo hardcode. State-page generation now merges `knowledge_db` evidence, distinguishes `REGULATED` vs `LIMITED` medical phrasing more conservatively, keeps `UT/NE` as regulated medical, and keeps `KS/WI/WY` as limited medical. Narrow validation PASS: `node tools/generate_country_page_storage.mjs` and `node tools/status-engine/recolor_wave_1.mjs` with `override_count=17` and `Reports/status-engine/status-review-required.md` reduced to `2` rows (`AL`, `HU`). Country snapshot runtime now derives country `mapCategory` from the same country-page storage path as popup cards, which fixes the last Algeria (`DZ`) snapshot/view mismatch while keeping canonical `medical.status=ILLEGAL`. Validation PASS: `npm -w apps/web run test -- src/lib/countryPageStorage.test.ts src/lib/truth/mapTruthDataset.test.ts src/lib/countryStatusDerivation.test.ts` and final `bash tools/pass_cycle.sh` with `POST_CHECKS_OK=1` and `HUB_STAGE_REPORT_OK=1`.
Now: Docs/spec review completed for `AGENTS.md`, `README.md`, `docs/PLAN.md`, `docs/CONTRACT.md`, `docs/ARCHITECTURE*.md`, `docs/PROJECT_ATLAS.md`, `docs/DEV.md`, `docs/OPS.md`, `docs/REPO_POLICY.md`, `docs/STATUS_ENGINE_AUDIT.md`, `docs/API.md`, `docs/DATA_ADDING.md`, `docs/VERSIONING.md`, `docs/MOBILE_GUIDE.md`, `docs/MONETIZATION.md`, and `docs/ROADMAP_NEXT.md`. Dev server is running and responds on `http://127.0.0.1:3000/new-map` and `/wiki-truth`. `Reports/popup-profile-audit.json` is at `300/300` processed entities with `60` rows marked `no_individual_wiki_page`, `2059` empty non-summary sections, `0` template sections, `0` repeated-text findings, `0` garbage-text findings, `0` raw URLs, `0` source errors, `0` status/color mismatches, and `23` status-review override rows now surfaced explicitly in the audit. `Reports/knowledge-harvester/first_wave_validation.json` now carries `cultivationCount` and `marketCount` columns; `Reports/knowledge-harvester/first_wave_validation.md` renders the same columns in the summary table. `Reports/popup-profile-full-run.txt` now records `cache_only=1`, `worklist_total=300`, `batch_total=300`, `processed=300`, `changed=7`, `garbage_text=0`, `source_errors=0`, and `status_review_overrides=23`. `npm run where` now enforces the workspace/top-level contract and succeeds under `ALLOW_SCOPE_OVERRIDE=1` in the current wide worktree. Mandatory full validation completed with `CI=PASS`, `Smoke=PASS`, `POST_CHECKS_OK=1`, and `HUB_STAGE_REPORT_OK=1`.
Open questions: UNCONFIRMED
