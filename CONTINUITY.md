Goal: Keep one canonical `/new-map` MapLibre runtime across `/`, `/new-map`, and `/c/[code]`, with CountryPageData as the only SEO/map/card truth, stable nearby/social-reality enrichment, exact non-regressing map visuals, truthful local overlays, and a live local AI companion that stays honest about model quality.
State: checkpoint=.checkpoints/20260518-000940.patch; CI=PASS; Smoke=PASS
Done: Route/map/SEO truth remains locked on the unified `/new-map` stack. The AI companion remains single-model on `qwen2.5:1.5b` with no runtime fallback switching. Context is lightweight v2 with short history, location anchoring, deterministic retry/final guards, and deterministic engines for nearby plus fallback truth blocks. Nearby AI integration formats truth-engine output as 4 ranked places with effective km, access type, destination `Risk:`, and the mandatory border warning. Slang/culture/chat/legal now stay in one LLM-first companion loop: router hints enrich prompt, follow-up intents carry into context, geo is disabled for slang/chat, CPU false-stop for Ollama is removed, intent inheritance now upgrades `general` into carried `legal/culture` where needed, and UI now prefers the validated final answer over concatenated retry stream text. Deterministic fallback stays only after failed validation. GEO/safety metadata is no longer displayed in the UI unless a real safety note is attached. Canonical production host is now `https://www.islegal.info`: metadataBase, Open Graph URLs, sitemap builders, both `robots.txt`, middleware, and host redirect now point to `www`, with non-www redirecting to `https://www.islegal.info`. Targeted metadata/sitemap tests pass, lint is green, and local production build passes. Deployment commit `c25f151` is pushed to `origin/main` with tags `good/20260430-125146` and `prod/20260430-www-canonical`. Source-quote sanitation is fixed in both the visible SEO page block and the serialized `seoCountryData` client payload; deployment commits `86e7f23` and `e6c08d2` are pushed to `origin/main`, and live HTML checks confirm `www` sitemap URLs plus clean Wyoming source quotes without the leaked wiki `style=\"background:#C4C9CD;\" |` preamble. False `TAG_NOT_PUSHED` is fixed in `tools/commit_if_green.sh`: the old `git ls-remote | grep -q` check was broken by `pipefail`/SIGPIPE, and the new targeted tag-ref helper now publishes commit `a1b068b` with `HEAD_PUSHED=1`, `TAG_PUSHED=1 name=good/20260430-132759`, and `TAG_PUSHED=1 name=prod/20260430-tag-verification-fix`. Latest deploy commit `33ee395` is pushed to `origin/main` with tags `good/20260430-134227` and `prod/20260430-ssot-ledger-sync`. Bing Webmaster verification meta tag `msvalidate.01` was added to the shared root layout head. The `apps/web npm run dev` entry now uses the singleton-safe `tools/ui/ui_dev_ssot.sh` wrapper, so it exits cleanly with `UI_ALREADY_RUNNING` when port 3000 is occupied instead of crashing on `EADDRINUSE`. Sitemap multi-delivery is live on `www` with five delivery URLs, and middleware now forces `Cache-Control: no-store` on sitemap paths while robots no longer blocks `/api/sitemap` or query-based sitemap delivery. Final live validation on 2026-05-04 confirms full edge propagation: `https://islegal.info/sitemap.xml` performs exactly one redirect to `https://www.islegal.info/sitemap.xml`, `sitemap.xml`, `sitemap-index.xml`, and `api/sitemap` now return `200` with XML content and `Cache-Control: no-store`, and `robots.txt` exposes all sitemap URLs without `Disallow: /api/` or `Disallow: /*?*`. Follow-up sitemap content fix is also live: deployment commit `fc45c70` restores full URL sets to `sitemap.xml`, `sitemap-main.xml`, and `api/sitemap` (311 URLs each), while `sitemap-index.xml` now points at the real child sitemaps for countries, states, and i18n coverage. Local mobile runtime QA scaffolding now exists under `apps/web/tests/mobile/` with deterministic viewport matrix, screenshot capture into repo-level `QA/mobile/`, local Lighthouse mobile coverage, viewport-fit/safe-area guards, keyboard mock hooks, and `apps/web/playwright.mobile.config.ts`. On 2026-05-16 the production-local mobile QA path was tightened with `viewport-fit=cover`, safe-area CSS vars, visual-viewport keyboard metrics, dock-height observers, popup clamp fixes, internal chat-only auto-scroll, compact keyboard-open dock behavior, localhost analytics suppression, idle-deferred `/new-map` runtime helpers, `/new-map` IP-hint suppression, repo-root artifact path resolution, and provided-throttling Lighthouse for local mobile audit. Focused reruns closed the last blockers for `iphone-landscape-webkit` AI keyboard/layout, `iphone-landscape-webkit` pan stability, and Android mobile performance. The mobile matrix is now expanded with `iphone-12-mini-webkit`, `iphone-15-pro-max-webkit`, `pixel-8-chrome`, and `galaxy-fold-closed-chrome`; the fold double-tap map target is now projection-based instead of hard-coded, and Android Lighthouse uses a dedicated warmed `android-chrome-perf` project plus a short settle gap without lowering thresholds. Full production-local mobile QA is now green inside `pass_cycle` with `MOBILE_QA_START=1`, `MOBILE_QA_OK=1`, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, and `PASS_CYCLE_EXIT rc=0 status=PASS`. `tools/pass_cycle.sh` now hard-gates PASS on production-local mobile QA after lint and build, starts its own local `next start` instance on an isolated port for the mobile suite, emits `MOBILE_QA_START=1` plus `MOBILE_QA_OK=1` or `MOBILE_QA_FAILED=1`, and blocks `POST_CHECKS_OK=1` / `HUB_STAGE_REPORT_OK=1` on early build/mobile failures. Production deploy is now locked to the same gate via `tools/deploy_prod.sh` / `npm run deploy:prod`, `tools/commit_if_green.sh` now explicitly requires `MOBILE_QA_OK=1` and `PASS_CYCLE_EXIT rc=0 status=PASS`, and ops docs now define the single-runtime mobile-first deploy contract. On 2026-05-17 the deploy payload/function-size reduction stayed effective in live deploy flow: root `.vercelignore`, `outputFileTracingExcludes` in `apps/web/next.config.ts`, `tools/vercel_prebuilt_prod.sh`, and `.vercel/**` ESLint ignores keep `vercel build --prod` under size limits, `.vercel/output` at `5.1M` across `201` files, and the largest generated functions around `0.37 MB`. Android local Lighthouse is re-stabilized without lowering budgets by warming the dedicated Lighthouse Chrome process once before the measured pass; the latest green summary is `performanceScore=0.87`, `accessibilityScore=1`, `CLS=0`, `LCP=653ms`. Live production mobile QA against the old prod commit is no longer current: `origin/main` is now `aafdea5`, and live prod switched to the same commit with `viewport-fit=cover` restored. A fresh ad-hoc mobile smoke for the live homepage `https://www.islegal.info/` now passes `5/5` across `android-chrome-home`, `iphone-se-home`, `iphone-14-home`, `ipad-mini-home`, and `iphone-landscape-home`. The homepage route `/` is the same runtime as `/new-map`, and the prod-home smoke confirms correct render, no horizontal overflow, visible AI dock, working map pan, successful France popup open, and successful popup close on all five devices. The prod-home artifacts are stored in `QA/mobile/prod-home/summary.json` plus per-device screenshots. AI text-entry was intentionally excluded from that prod-home smoke because the input is intentionally closed on live prod. The remaining `/new-map` prod-mobile red zone now has a live fix: prod serves commit `e67ea271641346886f3e2ee3e55d1a9d2958d7c6` with the early debug-map publication, popup helper stabilization, visible map surface, compact pre-`mapReady` loading shell, and CDN-friendly basemap cache headers. Focused live prod mobile QA for `/new-map` is green again: `android-chrome`, `android-chrome-perf`, and `iphone-se-webkit` passed `11/11`, and the fresh prod Android perf artifacts show runtime `lcp=3212`, Lighthouse `performanceScore=0.9`, `accessibilityScore=1`, `CLS=0`, and `LCP=1441.121`. AI text-entry remained intentionally excluded from the live prod checks. On 2026-05-17 a compact-width fold guard was added to the SEO overlay layout, `ViewportCountryPopup` rotate settle was hardened, local `pass_cycle` returned to green with `55 passed (7.1m)` in mobile QA and final markers `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, `PASS_CYCLE_EXIT rc=0 status=PASS`, and the shared Playwright helpers now include popup touch+mouse fallback plus desktop-safe perf popup handling. Production-local performance QA is now green after simplifying data/cache behavior and harness timing to reflect real interaction readiness: `perf` passes `26/26` with `2` expected skips, `/api/new-map/countries` is reduced to the simplified runtime source with CDN-friendly cache headers, country/card/country-page APIs reuse stable in-process snapshots, popup rotate clamps against the actual AI dock rect, and the latest authoritative `pass_cycle` is green with mobile QA `55 passed (4.7m)`, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, and `PASS_CYCLE_EXIT rc=0 status=PASS`.
Now: live prod commit `33e1db9` is rejected for Antarctica: user iPad Pro screenshots confirm the south-polar mask hides the continent by blending it into sea. Local corrective state removes the app Antarctica mask entirely, keeps `AQ` out of the interactive legal-fill source, and restores the basemap style contract to `background=land` plus `water=water`, so Antarctica stays visible without becoming app-owned clickable/fill geometry. The mandatory mobile viewport suite now includes an iPad Pro `1024x1366` regression that fails if `new-map-antarctica-mask` returns, if `AQ` enters `legal-countries`, if land/background and water colors collapse together, if the bottom polar viewport no longer reaches south latitudes, or if the bottom sample is covered by `legal-fill`; it also saves `ipad-pro-antarctica-visible` proof screenshots. Targeted production-local mobile viewport rerun is green (`13 passed`, `1 skipped`) and the proof screenshot is under `QA/mobile/local-restore-antarctica/iphone-12-mini-webkit/tablet-map-keeps-antarctica-visible-without-app-geometry-overlays-ipad-pro-antarctica-visible.png`. Full local `pass_cycle` is green with `MOBILE_QA_OK=1`, mobile QA `78 passed (5.2m)`, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, and `PASS_CYCLE_EXIT rc=0 status=PASS`. Link formatting fixes remain staged: internal isLegal links use dotted underline, external/source links use solid underline, popup source-list indentation is separated from inline Source links, and compact popup titles wrap cleanly. Prod deploy and prod proof are UNCONFIRMED until published/rerun. Prod AI text-entry remains intentionally closed; 403s from the locked assistant input path are not treated as this regression.
Open questions: UNCONFIRMED `deepseek-r1:1.5b` remains installed but is still rejected for runtime promotion on this host, and `deepseek-r1:7b` remains intentionally uninstalled on `8 GB RAM`. Other locally installed models may still exist in Ollama tags for diagnostics, but the product runtime default is intentionally single-model.
