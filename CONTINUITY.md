Goal: finalize production bypass stability spec/docs and produce a stability tag for current by-pass + perf baseline.
State: checkpoint=.checkpoints/20260615-131035.patch; CI=FAIL until Stage F; STORAGE_STATE_BYPASS_FLOW=IMPLEMENTED; PROD_ACCESS_REPROVED=YES; FULL_MATRIX_REPROVED=NO; GITHUB_DEPLOY_CONFIRMED=YES_FOR_93296fb; GPS_MARKER_FIX_LOCAL_PASS=YES; GPS_MARKER_DEPLOY_CONFIRMED=NO
Done: recovery tests PASS 30/30; `git diff --check` PASS; `npm run build` PASS; `.env.prod.local` created from user-approved PassKey; Stage A access probe `20260615T112745` PASS (`seed=200`, `nav=200`, `challenge_count=0`, `SECRET_LEAK_GUARD=PASS`); Stage B screenshot seed `20260615T112820` PASS (`SUCCESS_COUNT=1`, `CHALLENGE_COUNT=0`, screenshots saved); Stage C short matrix `20260615T112850` stopped before rows because warmup seed returned `403 challenge`; blocker saved to `Reports/vercel-bypass-recovery/stage-c-blocker.json`; cooldown Stage A `20260615T114443` PASS (`READY_FOR_SCREENSHOT_MATRIX`, `CHALLENGE_COUNT=0`, `SECRET_LEAK_GUARD=PASS`); cooldown Stage B screenshot seed `20260615T114520` PASS (`SUCCESS_COUNT=1`, `CHALLENGE_COUNT=0`, screenshots saved); Stage C short matrix `20260615T114556` stopped before rows with `challenge_count=1`, `ROWS=0`, `SECRET_LEAK_GUARD=PASS`; blocker updated at `Reports/vercel-bypass-recovery/stage-c-blocker.json`; cooldown Stage A `20260615T120144` PASS (`READY_FOR_SCREENSHOT_MATRIX`, `CHALLENGE_COUNT=0`, `SECRET_LEAK_GUARD=PASS`); added `tools/lib/vercel-bypass-session.mjs`, reusable `playwright/.auth/vercel-bypass.production.json` flow, state-aware access/screenshot/matrix runners, budget counters, diagnostics, and storageState/budget tests; storageState preflight PASS (`node --test` 38/38, `git diff --check` PASS, `npm run build` PASS); stateful Stage A `20260615T121046` PASS (`READY_FOR_SCREENSHOT_MATRIX`, `CHALLENGE_COUNT=0`, `STORAGE_STATE_WRITTEN=1`, `SECRET_LEAK_GUARD=PASS`); stateful Stage B `20260615T121058` PASS (`SUCCESS_COUNT=1`, `CHALLENGE_COUNT=0`, `STORAGE_STATE_USED=1`, `BYPASS_WARMUP_COUNT=0`); Vercel CLI deploy blocked (`VERCEL_TOKEN=0`, `~/.vercel/auth.json=0`, OIDC `403 text/html`); GitHub deploy path used: committed `93296fb` (`fix(prod): stabilize vercel bypass matrix state`) and pushed `main`; GitHub/Vercel status PASS for Production deployment `5066746098`, target `https://islegalcannabis-3l5f9fny0-krukovvs-projects.vercel.app`; post-deploy Stage A `20260615T151448` stopped at seed (`403`, `x-vercel-mitigated=challenge`, `STORAGE_STATE_WRITTEN=0`); blocker saved to `Reports/vercel-bypass-recovery/stage-a-post-deploy-blocker.json`; pending cooldown Stage A process stopped before it could make another production request because GPS marker bug superseded prod matrix; GPS marker root cause fixed locally by moving the rotated pin visual into child `[data-user-marker-pin="1"]`, preserving MapLibre marker classes with `classList.add`, and setting root marker `position:absolute`; GPS e2e PASS 5/5; `git diff --check` PASS; `npm run build` PASS; GPS marker fix committed/pushed as `608f220` (`fix(map): preserve GPS marker positioning`); GitHub/Vercel deployment for `608f220` did not appear in bounded polling (`statuses=[]`, `deployments=[]`, checks `[]`); no `VERCEL_TOKEN`, no `~/.vercel/auth.json`, no deploy hook in `.env.prod.local`.
Now: trigger deploy through GitHub path without touching app behavior, then verify production and resume staged prod matrix only after deployment is confirmed.
Open questions: UNCONFIRMED Whether GitHub/Vercel integration will create a production deployment for the GPS marker fix SHA after re-trigger; UNCONFIRMED whether Vercel challenge window clears after new deploy.
