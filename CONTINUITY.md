Goal: Keep the active MapLibre UI stable while turning `/wiki-truth`, map coverage, and truth-derived official-source ownership into a clean, source-level audit system with canonical wiki pages, non-shrinking registry floors, merge-safe notes, truthful CI/smoke, and a working SSOT diff/change pipeline.
State: checkpoint=.checkpoints/20260318-135209.patch; CI=PASS; Smoke=PASS
Done: Preserved the centralized `/wiki-truth` audit builder/view and pushed the fix down to source level. `apps/web/src/lib/wikiTruthNormalization.ts` now resolves expected wiki pages through a canonical resolver that prefers real `Cannabis_by_country` pages when explicitly discovered, otherwise falls back to canonical `wiki_claims_map` URLs or country-name SSOT titles. `apps/web/src/app/wiki-truth/page.tsx` and `apps/web/src/lib/wikiTruthAudit.test.ts` now build `expectedWikiPageByIso` through that resolver, and `apps/web/src/lib/wikiTruthNormalization.test.ts` locks the territory regressions (`AX`, `CW`, `BL`, `VI`, `BQ`, `CC`, `GF`, `RE`, `YT`, `VA`). `data/ssot/wiki_pages_universe.json` was rewritten from the canonical fallback path, removing pseudo-URLs such as `/wiki/land`, `/wiki/Cura_ao`, `/wiki/BQ`, `/wiki/CC`, `/wiki/GF`, and `/wiki/RE`. `tools/wiki/scrape_cannabis_by_country_universe.mjs` and `tools/wiki/scrape_cannabis_by_country_pages.mjs` now preserve the same source-level behavior on refresh. `tools/gates/wiki_pages_universe_shrink_guard.mjs` now fails on pseudo-URLs, not just shrink. `tools/gates/official_shrink_guard.mjs` now enforces both the filtered official-domain floor and the raw protected registry floor. Added `/trust-view` as a stable redirect to `/wiki-truth` in `apps/web/src/app/trust-view/page.tsx`; `tools/playwright-smoke/ui_smoke.mjs` now verifies that route. Added explicit contracts in `apps/web/src/lib/officialSources/registry.ts` and `apps/web/src/lib/notesMerge/mergeNotes.ts` with unit coverage for append/update-only registry behavior, floor protection, stronger-notes protection, and status-change delta metadata. `/wiki-truth` now renders `Official registry` and `Official geo coverage` as separate summary cards, so the protected registry universe is no longer conflated with row coverage. Vatican City now has a rich minimal note in `data/wiki/ssot_legality_table.json`, `data/wiki/wiki_claims/VA.json`, and `data/wiki/wiki_claims_map.json`, removing `WIKI_NOTES_MISSING` without weakening notes quality. `tools/ssot/metrics_core.js`, `tools/wiki/inspect_official.mjs`, and `tools/pass_cycle.sh` now expose the same separated universes in CI/output: protected registry floor, official geo coverage over valid wiki rows, and pseudo-URL / shrink guards in the same run. Added SSOT-Diff Engine modules under `apps/web/src/lib/ssotDiff/*`, `/changes` UI, `/api/ssot/changes`, recent-change block on `/wiki-truth`, a Node diff runner `tools/ssot/ssot_diff_run.mjs`, snapshot GC in `tools/ssot_snapshot_gc.mjs`, and drift guard in `tools/gates/ssot_drift_guard.mjs`. `pass_cycle` now runs truth tests including diff modules, then snapshot generation, GC, and drift checks before smoke. Restored the full normalized truth table on `/wiki-truth`: `WikiTruthTable.tsx` now renders `All countries truth table` from `audit.allRows` while `Audit mismatches` remains a separate section powered by `audit.mainRows`. Added `tools/gates/wiki_truth_table_guard.mjs` and extended smoke/tests so truth-table disappearance becomes detectable. The home map now uses an explicit 4-bucket truth-to-paint matrix in `apps/web/src/lib/truth/mapTruthDataset.ts`, restoring the yellow `LIMITED_OR_MEDICAL` category for `Illegal recreational + medical/limited` countries, exposing color-distribution diagnostics, and guarding against future collapse via `tools/gates/map_color_distribution_guard.mjs`. Official ownership is now aligned to SSOT again: `data/official/official_domains.ssot.json` gained real official domains for `GN`, `GW`, `HT`, `NI`, and `SS`; `apps/web/src/lib/officialSources/officialLinkOwnershipBuilder.ts` now resolves `domain_rule` country hits as `country_domain_match`; `data/ssot/official_link_ownership.json` was rebuilt to `raw=419`, `effective registry total=418`, and country coverage `195 / 201`, closing `OFFICIAL_SOURCES_MISSING` for `GN/GW/HT/NI/SS`. `/wiki-truth` now exposes clearer explainability stages and hygiene: `OfficialOwnershipSummary.tsx` separates raw registry, resolved rows, effective rows, filtered rows, excluded rows, and global regulatory references; `OfficialOwnershipTable.tsx` shows `Source type` and `Registry state`, keeps invalid/unknown rows visible, and renders `GLOBAL_FALLBACK` rows in a dedicated `Global regulatory references` section. `tools/ssot/metrics_core.js` now computes `OFFICIAL_GEO_ROWS_*` strictly from effective ownership projection instead of legacy `wiki_official_eval`, eliminating the previous second-truth mismatch in coverage reports. The map overlay contract was hardened again for the current drift task: `apps/web/src/lib/map/leafletInteractionRuntime.ts` now canonicalizes Leaflet overlay input to a single world, filters world-wrap/out-of-bounds features, reports overlay duplicate diagnostics, and normalizes sibling-overlay zoom with `LEAFLET_ZOOM_OFFSET=1`, matching MapLibre’s world scale instead of the old half-scale overlay. `apps/web/src/lib/maplibreCountryLayer.ts` no longer creates app-owned `hover/selected` MapLibre highlight layers, the business fill returned to a lighter state, and `apps/web/src/app/_components/NewMapLibreMap.tsx` now hides the interaction overlay during pan as well as animated zoom, while also using `fadeDuration=0`, `cancelPendingTileRequestsWhileZooming=false`, and `refreshExpiredTiles=false` to reduce label lag and catch-up during zoom. `NewMapLibreMap.module.css` keeps the overlay fully pointer-transparent so MapLibre remains the only hit owner and Leaflet is the only highlight renderer. `tests/map/map-load.spec.ts`, `tools/playwright-smoke/map_overlay_live_suite.mjs`, `tools/playwright-smoke/live_probe_lib.mjs`, `tools/gates/map_interaction_overlay_alignment_guard.mjs`, `tools/gates/map_overlay_duplicate_features_guard.mjs`, and `tools/gates/playwright_live_evidence_guard.mjs` now guard against duplicate overlay features, wrong Leaflet zoom parity, in-map debug badge regressions, non-zero center drift, stale zoom offsets, and require headed live evidence for `CA/RU/FR/IN/KZ/CN/US` on Chromium and WebKit. The Morocco / Western Sahara gap is now fixed at source assembly level: `apps/web/src/lib/mapData.ts` synthesizes an explicit map-render fallback for `EH` from existing wiki/centroid data instead of leaving raw basemap land white, `apps/web/src/lib/map/statusPalette.ts` centralizes the shared pastel map palette used by both MapLibre and Leaflet, `apps/web/src/lib/map/preparedCountrySources.ts` and `apps/web/src/app/_components/NewLeafletMap.tsx` now consume that shared palette, `apps/web/src/lib/maplibreCountryLayer.ts` softens fill/line opacity, and `apps/web/src/app/_components/NewMapLibreMap.tsx` restores SVG interaction overlay rendering so live diagnostics and tests see `interactiveCount=203` again. Interactive live proof on the active localhost dev server now shows `MA -> fillColor=#7bcf9f`, `EH -> fillColor=#cbd5e1`, `mapPaintStatus=UNKNOWN`, and `overlayFeatureCount=203` in `Artifacts/morocco-western-sahara-after.json`, with the corresponding screenshot in `Artifacts/morocco-western-sahara-after.png`. `apps/web/src/app/_components/NewMapLibreMap.tsx` now also hands downward wheel input back to page scroll when the map is already at full min zoom by disabling `scrollZoom` for that event on the wrapper capture phase and immediately re-enabling it afterward, which fixes the blocked page-scroll bug at full zoom-out without changing normal map zooming. `tests/map/map-load.spec.ts` now asserts that a wheel-down gesture over the map at full min zoom increases `window.scrollY`. The territory coverage contract is now extended from “all truth rows are paintable” to “all land geos on the map are paintable”: `apps/web/src/lib/mapData.ts` adds fallback country rows for every map geo missing from `legal_ssot`, `apps/web/src/lib/truth/mapTruthDataset.ts` now treats `UNKNOWN` as valid painted coverage instead of an unpainted hole, `apps/web/src/lib/truth/mapTruthDataset.test.ts` asserts coverage for `PR/FK/GS/AQ/NC`, and `tests/map/map-load.spec.ts` adds a live browser regression for those same territories. Live proof artifacts include `Artifacts/puerto-rico-after.png`, `Artifacts/falklands-after.png`, `Artifacts/antarctica-after.png`, `Artifacts/new-caledonia-after.png`, and `Artifacts/territory-coverage-runtime.json`, where `PR/FK/AQ/NC` all report `mapPaintStatus=UNKNOWN`, `truthLevel=UNKNOWN`, and non-zero polygon counts. The current USA-state fix now corrects the broken state truth/render path too: `apps/web/src/lib/mapData.ts` reconstructs `US-*` effective statuses from the raw wiki state-table color signal instead of the corrupted all-legal feed, `apps/web/src/lib/maplibreCountryLayer.ts` hides the parent `US` country fill once state choropleth takes over at state zoom through valid top-level zoom interpolations, and `apps/web/src/app/_components/NewMapLibreMap.tsx` refreshes `stateGeojsonData`, routes hover/click to rendered `US-*` features, and exposes state hover/selection lines. `tests/map/map-load.spec.ts` now verifies that `US-TX` renders as `LIMITED_OR_MEDICAL` with the shared yellow palette and that hover resolves to `US-TX` instead of `US`. The current follow-up perf polish now removes the expensive `move`/`zoom`/`render` overlay-sync hot path over the USA state view, so interaction sync settles on `moveend` instead of `render`, and softens the state hover line to `line-width=1.15` and `line-opacity=0.62`. Live artifacts for the state fixes are in `Artifacts/us-states-fix-after.png`, `Artifacts/us-states-fix-runtime.json`, `Artifacts/us-states-pan-perf-after.png`, and `Artifacts/us-states-pan-perf-runtime.json`. The latest localhost map regression fix keeps the safe mask/fill render architecture intact while restoring the real basemap under it: `apps/web/src/lib/maplibreStyle.ts` now anchors the thematic overlay before the first provider symbol layer instead of a narrower label bucket, `apps/web/src/app/_components/NewMapLibreMap.tsx` reports `basemapGeometryVisible`, `activeMarkersCount`, and `markerSource`, and the current-location marker now renders only for accurate GPS (`source === "gps"` and bounded `accuracyM`) instead of coarse IP/manual fallback. `tests/map/map-load.spec.ts` and `tools/gates/map_render_stack_contract_guard.mjs` now lock that contract, and fresh live artifacts are `Artifacts/basemap-geometry-marker-after.png`, `Artifacts/basemap-geometry-marker-zoom-after.png`, `Artifacts/basemap-geometry-marker-runtime.json`, and `Artifacts/basemap-geometry-marker-zoom-runtime.json`, where the active localhost runtime reports `basemapGeometryVisible=true`, `activeMarkersCount=0`, and `markerSource=null` in the default fallback state.
Now: Country geometry SSOT for countries now uses `data/geojson/ne_10m_admin_0_countries.geojson` through `apps/web/src/lib/mapData.ts`, while states stay on 50m. The active MapLibre pipeline keeps canonical interaction geometry and render-safe fill on the same country identity, Chongming and fresh WebKit Qiantang proof no longer show the earlier large land underfill, mandatory coastal proof exists for CN,BR,FR,MA,JP,ID,PH, seam smoke is green for CN,BR,FR,MA,US,CA,JP,ID,NO,GR,PH on Chromium and WebKit, and the simplification/runtime layer is now precomputed-first: `preparedMapPayload`, `preparedCountrySources`, `wikiTruthPageModel`, shared `buildStamp`, simplification/perf reports, log/file/perf/no-second-truth/vector-tile guards, and the experimental vector-tile benchmark path are all wired. The live map contract now covers both dropped admin0 specials and ordinary territories without SSOT rows: `ISO_A2=-99` lands no longer disappear, territories such as `PR`, `FK`, `GS`, `AQ`, and `NC` render as explicit `UNKNOWN` status coverage instead of white polygons, and the MapLibre renderer now paints all `US-*` state polygons with the same shared legality palette and comparable opacity as the country layer. The Stage-2 production render stack is now wired behind the existing safe geometry-source flag without changing the default `geojson` behavior: `apps/web/src/config/mapConfig.ts` still defaults to `MAP_GEOMETRY_SOURCE="geojson"` while keeping dormant vector metadata, `apps/web/src/lib/maplibreCountryLayer.ts` now exposes a hard render stack contract (`mask -> fill -> idle-outline -> hover`) with a white mask for both source types, a single country fill layer using soft RGBA expression output at `fill-opacity=1`, a transparent fill outline, a readable neutral idle line-layer, and dedicated hover/selected line accents. `apps/web/src/app/_components/NewMapLibreMap.tsx` now asserts the render stack order, exposes `hasMaskLayer/hasFillLayer/hasIdleOutlineLayer/hasHoverLayer/layerOrder/fillLayerCount/lineLayerCount` in `__MAP_DEBUG__`, keeps Cuba/Bahamas/Haiti/Nepal/Canada projection points live on localhost, and now also reports `basemapGeometryVisible`, `activeMarkersCount`, and `markerSource` so the active basemap-underlay and GPS-only marker contract are visible at runtime. `apps/web/src/lib/maplibreStyle.ts` now anchors the thematic overlay before the first provider symbol layer instead of a narrower detail-label subset, restoring visible land/water/terrain under the thematic fill without disturbing the safe stack. `tools/vector_tile/build_countries_tiles.sh`, `tools/gates/map_vector_tile_config_guard.mjs`, `tools/gates/map_render_stack_contract_guard.mjs`, and `tools/gates/map_interaction_overlay_alignment_guard.mjs` now lock the feature-flag/vector-source/render-stack/highlight-layer wiring, and `tools/pass_cycle.sh` runs the render-stack guard in the main CI path. Fresh live artifacts are `Artifacts/map-geometry-source-geojson-live.json`, `Artifacts/map-geometry-source-geojson-live.png`, `Artifacts/stage2-europe-outline-after.png`, `Artifacts/stage2-caribbean-outline-after.png`, `Artifacts/stage2-south-asia-outline-after.png`, `Artifacts/basemap-geometry-marker-after.png`, `Artifacts/basemap-geometry-marker-zoom-after.png`, `Artifacts/basemap-geometry-marker-runtime.json`, and `Artifacts/basemap-geometry-marker-zoom-runtime.json`, where `geometrySource=geojson`, `hasMaskLayer=true`, `hasIdleOutlineLayer=true`, `layerOrder=[ilc-choropleth-mask, ilc-choropleth-fill, ilc-choropleth-line, ilc-choropleth-hover-line]`, `fillLayerCount=1`, `lineLayerCount=3`, `basemapGeometryVisible=true`, `activeMarkersCount=0`, and `markerSource=null` on the default localhost fallback state. `bash tools/pass_cycle.sh` has been rerun after this latest basemap/marker patch, and `Reports/ci-final.txt` now confirms `CI_STATUS=PASS`, `FAIL_REASON=NONE`, `POST_CHECKS_OK=1`, and `HUB_STAGE_REPORT_OK=1`.
WIP: Post-Stage-2 regressions are now resolved without rolling back the safe stack: `apps/web/src/lib/maplibreStyle.ts` anchors the thematic layers below provider detail labels (`place_other` first), `apps/web/src/app/_components/NewMapLibreMap.tsx` reconciles one ordered country/state render stack, keeps `US-*` state layers above country fill, adds state-aware `projectGeo()` for live hover routing, and restores wheel ownership with an explicit handoff arm so Chromium/WebKit zoom stays on-map until the real zoom limit. `tests/map/map-load.spec.ts` now covers basemap detail labels, US state fills, US state hover routing, Cuba mapping, and the min-zoom wheel handoff; fresh live artifacts are `Artifacts/post-stage2-europe-mena-detail-after.png`, `Artifacts/post-stage2-caribbean-after.png`, `Artifacts/post-stage2-us-states-after.png`, and `Artifacts/post-stage2-webkit-wheel-after.png`, with matching runtime JSON files proving `detailLayerAnchorId=place_other`, visible `US-CA/US-NV/US-OR`, and WebKit wheel zoom before page scroll at `minZoom`. UNCONFIRMED whether the existing tool-level `tools/wiki/sync_all.mjs` should be refactored to import the shared `apps/web/src/lib/notesMerge/*` contract directly, or whether duplicated logic is acceptable as long as tests and guards keep both paths aligned.
Open questions: UNCONFIRMED whether `/wiki-truth` should expose a first-class territories coverage section instead of keeping territory-only misses inside diagnostics/SSOT sections, and UNCONFIRMED whether official registry floors should eventually be surfaced directly in the audit UI rather than only in guards/reports.
