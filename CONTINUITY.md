Goal: Keep `/new-map` on one native MapLibre pipeline with exact reference RGB colors, visible admin1 borders, visible Antarctica without hover, visible native water geometry, pastel US states with state-level statuses, no white flash, no cache drift, no seam regression, while shipping global analytics safely through the app root, mounting the same map runtime on `/`, hiding production-only debug/update overlays from users, and keeping a zero-overhead Antarctica-only ASCII overlay runtime on local.
State: checkpoint=.checkpoints/20260408-230335.patch; CI=PASS; Smoke=PASS
Done: The missing regional borders were traced to missing data, not weak paint: the custom `admin-boundaries` GeoJSON path returned only `50` US state features, so it could never draw `RU/DE/FR/PL/NO`. `apps/web/src/new-map/createMap.ts` now points `admin-boundary-line` at the real native vector source already present in the basemap style: `source=\"carto\"`, `source-layer=\"boundary\"`, `filter=[\"all\", [\"in\", \"admin_level\", 4, 6], [\"==\", \"maritime\", 0]]`, placed between country fills and labels. The dead world-admin fetch path was removed from `MapRoot`/`NewMapBootResult` data flow, so the render pipeline is simpler again. Live Playwright-interactive WebKit runtime confirmed `admin-boundary-line` is non-empty (`renderedCount=8` in the tested camera, `source=carto`, `sourceLayer=boundary`, sample feature `admin_level=4`, `maritime=0`), and the visual pass showed internal borders again inside `RU`, `DE`, `FR`, `PL`, and `NO`. `rootBackground == style.background == style.water == #d7dcdc` remained unchanged, flash stayed green (`0` white on zoom-out, `~0.00098-0.00107` on baseline/zoom-in), and no north seam return appeared in Arctic checks. `createMap.ts` no longer pulls `/us-states.geojson` directly and instead uses a native `/api/new-map/us-states` snapshot built by `buildUsStateSourceSnapshot()`. The latest Antarctica visibility fix confirmed the issue was not a layer filter: `legal-fill` has no AQ-excluding filter and runtime still rendered `aqRenderedCount=2`, but `AQ` had been using the exact same `UNKNOWN` fill as water/background (`#d7dcdc`). `buildCountrySourceSnapshot()` now keeps `AQ` in the same countries layer and assigns it a slightly darker neutral unknown fill (`#c5ccd3`, hover `#d4dae0`), so the continent is visible without hover while the global water/background stays unchanged. `apps/web/src/app/layout.tsx` now mounts Yandex.Metrika globally for all pages through `next/script` with `strategy=\"beforeInteractive\"` plus the provided `noscript` beacon, so analytics load once at the app root instead of being duplicated per page. The latest lakes/rivers fix confirmed native water geometry had never been removed from `basemap-style`: `water`, `water_shadow`, and `waterway` were still present and visible, but `legal-fill` sat above them and masked inland water. `createMap.ts` now moves native carto water layers above countries and below `admin-boundary-line`, and live runtime order is `legal-fill < water/water_shadow/waterway < admin-boundary-line`; a Baikal probe now returns rendered `water`/`water_shadow` hits on top of `RU`, which restores inland water geometry without changing palette or reintroducing flash/seam regressions. The latest prod-drift hardening aligned more of the `/api/new-map` surface with strict SSOT cache policy: `basemap-tile` now exports `dynamic=\"force-dynamic\"`, `revalidate=0`, fetches upstream tiles with `cache:\"no-store\"`, and returns `Cache-Control: no-store, no-cache, must-revalidate`; the client fallback fetch for `new-map-card-index.json` also dropped `force-cache`, so browser cache is no longer allowed to become the truth source for popup/runtime metadata when the prefetch path is unavailable. The production state-color regression was closed via two bundle-safe fixes: `apps/web/src/lib/usStateWikiTable.snapshot.ts` now pins the US wiki-table recreational color rows inside the app bundle, and `apps/web/src/new-map/countrySource.ts` applies `deriveUsStateStatusOverrideFromWikiTable()` directly inside `/api/new-map/us-states` before resolving pastel colors, so `US-TX` renders `LIMITED_OR_MEDICAL` and `US-ID` stays `ILLEGAL` on prod regardless of downstream `Legal/Illegal -> green` canonicalization. GitHub→Vercel autodeploy is now confirmed on prod at commit `0fbb51ba7156d47e31d19956529ac805c4507d9a`. Production HTML also contains the injected Yandex.Metrika script `https://mc.yandex.ru/metrika/tag.js?id=108419114`, `ym(108419114, 'init', ...)`, and the `watch/108419114` noscript beacon. The temporary production AI-bar lock is now deployed via commit `2ed39e30d0d0aa464d3f048ca7913ed25dc4f9e7`: `apps/web/src/new-map/components/AIBar.tsx` disables the input and submit path when `NODE_ENV === "production"`, and live hydrated prod DOM on `https://islegal.info/new-map` now reports placeholder `AI assistant temporarily unavailable`, `disabled=true`, `readOnly=true`, `aria-disabled=true`, and `submitDisabled=true`. `apps/web/src/new-map/MapRoot.tsx` now gates the two debug/runtime cards behind `runtimeIdentity.runtimeMode !== \"production\"`, so prod users no longer see the `New Map Skeleton` and `Runtime` overlay while local retains them for debugging. `apps/web/src/plugins/buildWatcher.tsx` now also short-circuits in production (`process.env.NODE_ENV === "production"`), so the global `Доступно обновление` / `Обновить` / `Позже` banner is suppressed on prod while local dev keeps the watcher behavior. GitHub→Vercel autodeploy has published the root map reuse on prod at commit `b55d7518932f328296206d38393894da8661a272`; live `/` and `/new-map` both now prefetch `/api/new-map/basemap-style`, `/api/new-map/countries`, and `/new-map-card-index.json`, and the old root `Map temporarily removed for rebuild` placeholder is gone. The local ASCII overlay runtime is now scaffolded under `apps/web/src/new-map/ascii/`: `AsciiEngine` runs a canvas-only RAF loop with actor cap `<=40`, deterministic scenario picking, Antarctica geo-gating, idle/hover/click trigger queueing, and three starter registry scenarios (`story-420`, `smoke-circle`, `dance-wave`). `MapRoot` mounts the overlay canvas above the map surface and binds a lightweight `map -> geoStore/trigger` bridge via `moveend/mousemove/click`, without React state or direct engine↔MapLibre coupling. Local `npm -w apps/web run build` is green. Visual local Antarctica verification is now confirmed in Playwright-interactive WebKit: `/new-map` reached `data-map-ready=1`, the map was moved to `lat=-77,lng=0,zoom=3.4`, the overlay canvas stayed non-empty before and after trigger (`nonEmpty 483 -> 192` in sampled frames), and the canvas alpha bounds landed in the lower-left sector (`x=0..71,y=89..130` on a `300x150` overlay), with a direct extracted canvas frame showing rendered ASCII glyphs.
Now: Local `/` and `/new-map` both resolve to the same new-map server entrypoint; local keeps the debug/runtime overlay cards, the build-update watcher, and the full AI bar for development, and mounts an isolated canvas ASCII overlay runtime that stays dormant outside Antarctica and wakes only via idle/hover/click trigger queueing. Prod `/` and `/new-map` keep the same map pipeline and analytics but serve a locked AI input with disabled submit, hide the debug/runtime overlay cards from users, and suppress the global build-update banner. Local and prod both still use one `/api/new-map/us-states` pipeline with bundle-safe wiki color overrides, native `carto/boundary` for admin borders, native inland water geometry above country fills, an Antarctica fill that no longer collapses into the water color, and strict `no-store` policy across the proxied `/api/new-map` surface.
Open questions: UNCONFIRMED strict `ΔRGB ≤ 2` on the Germany sample only; water, AU, RU, and India matched the reference sample values directly in Playwright-interactive WebKit.
