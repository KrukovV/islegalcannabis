Goal: Improve `/new-map` PageSpeed/TBT without regressing map load, labels, hover, popup, colors, or GPS.
State: checkpoint=.checkpoints/20260616-123719.patch; CI=PASS; Smoke=PASS
Done: Baseline live PSI before this perf pass recorded `desktop score=60 mobile score=55`; moved the heavy `@/lib/countryCardEntry` builder off the initial `/new-map` route path and into an on-demand popup fallback import inside `apps/web/src/new-map/MapRoot.tsx`; rebuilt and confirmed `/new-map` entry JS dropped from 5 chunks / `1206152` bytes to 4 chunks / `1106172` bytes (`-99980 bytes`, `-97.6 KiB`); hardened the prod bundle by compile-time stripping the debug overlay/runtime badge path when `NODE_ENV=production`; local `measure_new_map_js_city` stayed functionally green with first-party script transfer `1207.8 KiB -> 1197.1 KiB` (`-10.7 KiB`) and city-label time `747ms -> 364ms` (`-383ms`); local `measure_new_map_gps_flow` stayed green with `marker=24ms center=31ms recenter=11ms`; oversized perf screenshots and intermediate reports were moved out of repo to `~/islegalcannabis_archive/20260616-perf-artifacts/`; safe bundle-cut patch shipped to prod in commit `5e7dc37`; live PSI after on `5e7dc37` measured `desktop score=65 TBT=669ms LCP=660.5ms` and `mobile score=69 TBT=543ms LCP=3231ms`; final `bash tools/pass_cycle.sh` passed with `POST_CHECKS_OK=1` and `HUB_STAGE_REPORT_OK=1`; manual mobile/device failures on `2026-06-16` were resolved by syncing `e2e/check.mobile.spec.ts` to the current `/check` verify markup, moving `e2e/mobile-cold-start.spec.ts` onto the active QA route `/new-map?qa=1` plus `new-map-surface` readiness, and updating `tools/playwright-smoke/ui_smoke.mjs` from the stale placeholder contract to the live home `/new-map` surface contract; rerun status after fixes: Playwright mobile `2/2 PASS` and `ui_smoke` matrix `4/4 PASS` with `UI_SMOKE_OK=1`, `UI_SMOKE_WEBKIT_OK=1`, `FULL_SCREEN_HOME_OK=1`, `FULL_SCREEN_HOME_MOBILE_OK=1`, `HOME_SINGLE_SCREEN_OK=1`, `HOME_DOCUMENT_SCROLL_OK=1`, `NEW_MAP_SURFACE_OK=1`, `RUNTIME_PARITY_OK=1`; GitHub-tracked history and local docs were audited for mobile devices, and `docs/MOBILE_GUIDE.md` now records both the current active viewport matrix and the historical named-device matrix recovered from May 2026 configs/tags, including `ipad-pro-chrome`, `ipad-mini-webkit`, `pixel-8-chrome`, `galaxy-fold-closed-chrome`, `iphone-12-mini-webkit`, and `iphone-15-pro-max-webkit`; the historical matrix has now been restored as the executable config `apps/web/playwright.mobile.config.ts` plus `apps/web/tests/mobile/mobile-device-matrix.spec.ts`; `npm run lint` is green again after removing the synchronous popup fallback reset from `MapRoot.tsx` and replacing `any` mocks in `createMap.test.ts`; `npm run mobile:qa` on `http://127.0.0.1:3000` passed `41` tests with `9` expected skips across `android-chrome`, `iphone-se-webkit`, `iphone-14-webkit`, `pixel-8-chrome`, `galaxy-fold-closed-chrome`, `ipad-mini-webkit`, `ipad-pro-chrome`, `iphone-landscape-webkit`, `iphone-12-mini-webkit`, and `iphone-15-pro-max-webkit`, with `ipad-pro-chrome` covering the extra rotation regression `5/5`
Now: UNCONFIRMED only whether a later non-test pass should continue chasing `Speed Index`; the repo now has both the documented and executable named-device mobile matrix.
Open questions: UNCONFIRMED whether a later pass should target `Speed Index` specifically, since score/TBT improved materially but SI remains the least attractive lighthouse metric on both desktop and mobile.
