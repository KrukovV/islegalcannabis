Goal: Complete a 100% visual + data popup/wiki audit for all 307 map entities, keep popup and SEO content aligned with Wiki-backed sources, and remove resolver gaps that leave runtime popups without canonical wiki references.
State: checkpoint=.checkpoints/20260628-140823.patch; CI=PASS; Smoke=PASS
Done: Deployed commit `273a3fc` to `origin/main` via `tools/commit_if_green.sh`; moving release tags now point at the same commit: `good/20260628-092605` and `prod/stable`. This supersedes the earlier intermediate deploy `d6508ce` and includes the build fixes that unblocked `next build`: the type-narrowing cleanup in `apps/web/scripts/popup-visual-audit-full.ts`, the nullable profile fix in `apps/web/src/lib/countryPageStorage.ts`, and refreshed `Artifacts/popup-visual-audit/full-manifest.json` / `full-summary.json` / `full-index.html` / `full-validation.json` so the full guard is no longer stale. `ViewportCountryPopup` compaction remains fixed generically. Wiki section detection no longer depends only on heading names: semantic classification now recognizes real content sections such as `Background`, `Developments`, `Early history`, and `Modern accounts`, while skipping non-content blocks like `References` and `See also`. The canonical resolver/extractor fix remains in place: ambiguous generic cannabis titles such as `Cannabis in Georgia` are canonicalized to the specific country/state page when upstream sources prove it, dedicated articles with rich lead text but weak heading structure can still yield semantic wiki sections from lead paragraphs, and popup cleanup drops unattributed quote fragments. This removed false sparse classifications on real dedicated Wiki pages such as `AM`, `KP`, `AR`, and the Sweden quote fragment from runtime popup content. Full popup/wiki audit remains `307/307`; current repo copies of `Artifacts/popup-visual-audit/full-manifest.json`, `full-report.csv`, `full-summary.json`, `full-index.html`, `full-validation.json`, and `sparse-wiki-text-report.md` are fresh, while heavy screenshot/html payload now lives at `/Users/james/islegalcannabis_archive/20260628-popup-audit-refresh` and manifest rows point to those real archived files so storage guards stay green. The legacy 300-row `popup-profile-audit` still uses the same canonical cannabis source resolver; current audit is `processed=300/300`, `source_errors=0`. The full guard still enforces screenshot pairs, manifest freshness, section-comparison schema, no raw URLs, no repeated text across semantic sections, no fake thematic sections for root/no-page cases, and no section-count regressions for the baseline 300-row set. New local fix closes the `GE` vs `US-GA` cross-contamination at two layers: `tools/knowledge/harvest_cannabis_knowledge.mjs` now drops ambiguous generic cache-title collisions and treats generic-vs-disambiguated cannabis titles from the same article family as replaceable during merge; targeted re-harvest rewrote `data/cannabis_profiles/knowledge_db.json` / `first_wave_profiles.json` so `GE` now uses `Cannabis_in_Georgia_(country)` while `US-GA` uses `Cannabis_in_Georgia_(U.S._state)`. Rebuilt `data/countries/*.json` cleared the stale country-level `notes_raw` leak from `data/countries/us-ga.json`, which had been surfacing in `/c/us-ga` source quotes. Project docs/spec are now refreshed in `README.md`, `docs/CONTRACT.md`, `docs/DEV.md`, and `docs/PLAN.md` to document the 307-GEO full-audit contract, stale-manifest guard, archive policy for heavy audit payloads, and the generic ambiguous-title resolver rule for popup + SEO content.
Now: Local evidence after the Georgia fix is fresh. Focused tests passed: `node --test tools/knowledge/harvest_cannabis_knowledge.test.mjs` (`61/61`) and `npm exec -- vitest run src/lib/countryPageStorage.test.ts` in `apps/web` (`31/31`). Targeted popup visual audit on the live singleton dev server passed for `GE,US-GA`: `NEW_MAP_POPUP_VISUAL_AUDIT=1 NEW_MAP_POPUP_VISUAL_AUDIT_GEOS=GE,US-GA PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm exec -- playwright test e2e/new-map.popup.spec.ts -g "visual/wiki audit emits"`. Full `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm -w apps/web run popup:visual:audit:full` completed successfully with `total=307 popupCaptured=307 wikiCaptured=307`. Final `bash tools/pass_cycle.sh` passed, and `Reports/ci-final.txt` contains `POST_CHECKS_OK=1` and `HUB_STAGE_REPORT_OK=1`.
Open questions: UNCONFIRMED
