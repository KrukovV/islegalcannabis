Goal: finalize production bypass stability spec/docs and produce a stability tag for current by-pass + perf baseline.
State: checkpoint=.checkpoints/20260615-010401.patch; CI=PASS; Smoke=PASS
Done: `apps/web/src/app/layout.tsx` now keeps critical preconnect/preload hints and removed inline `countries` prefetch script execution in document head; `apps/web/src/new-map/MapRoot.tsx` now fetches `countries` on mount via `loadCountries` and no longer reads `__NEW_MAP_PREFETCH__`; `apps/web/src/plugins/buildWatcher.tsx` removed prefetch-cache reset paths; `apps/web/src/app/new-map/page.config.test.ts` contract updated to enforce no head inline prefetch while keeping preconnect/preload and preconnect count; `docs/VERCEL_BYPASS.md` and `docs/CONTRACT.md` updated to codify mount-time countries fetch; local `Reports/network-tree/after-prefetch-inline-local.chromium.json` and `Reports/new-map-payload/after-prefetch-inline-local-comp.chromium.json` captured before/after deltas and production live `pass_cycle` completed with `PROD_LIVE_OK=1`, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`; `Reports/prod-live-gate/latest.json` and `Reports/vercel-bypass-live/method2_api_cookie_seed.png` are current.
Now: final regression comparison done, ready to commit and hand off final report.
Open questions: UNCONFIRMED Whether to force `PROD_EXTENDED_TAIL_GATES=1` on the next pass for full prod payload/JS/GPS evidence.
