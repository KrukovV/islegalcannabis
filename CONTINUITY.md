Goal: finalize production bypass stability spec/docs and produce a stability tag for current by-pass + perf baseline.
State: checkpoint=.checkpoints/20260615-005033.patch; CI=PASS; Smoke=PASS
Done: `apps/web/src/app/layout.tsx` includes preconnect/preload; `apps/web/src/app/new-map/page.config.test.ts` and `tools/vercel_bypass.test.mjs` contract tests pass; `docs/OPS.md` + `docs/VERCEL_BYPASS.md` updated; local payload/network/JS/perf artifacts re-measured; moved `maplibre-gl` stylesheet import from `apps/web/src/app/globals.css` into `apps/web/src/new-map/MapRoot.tsx` to remove render-blocking CSS from global route entry; split `apps/web/src/new-map/MapRoot.tsx` map runtime initialization through dynamic `apps/web/src/new-map/mapRuntime.ts` import to defer maplibre JS/CSS until mount; completed local before/after JS payload delta collection and baseline comparison (`total 2297.6→2121.7 KiB`, `scripts 1388.6→1213.6 KiB`, unused first-party script transfer 0.6→0 KiB); produced pass-key live probe evidence in `Reports/vercel-bypass-live/last_run.json` and `Reports/prod-live-gate/latest.json`; ran `bash tools/pass_cycle.sh` with `VERCEL_AUTOMATION_BYPASS_SECRET='mHHbgbRdY1TnZgmSnLBgZ7w5jVVAXC41'` and got `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, but final `CI_STATUS=FAIL` because `prod_live_quality_gate` hit `SEED_STATUS_HIGH:403` (`Vercel Security Checkpoint`).
Done: `bash tools/vercel_bypass_live_probe.mjs` and `tools/prod_live_quality_gate.mjs` were retried and succeeded; final `bash tools/pass_cycle.sh` now passes with `CI_STATUS=PASS`, `PROD_LIVE_OK=1`, `PROD_EXTENDED_TAIL_SKIPPED=1 reason=PROD_BUDGET_DEFAULT`.
Now: commit docs/spec updates and prepare deployment handoff with green artifacts.
Open questions: UNCONFIRMED Whether to force `PROD_EXTENDED_TAIL_GATES=1` on the next pass for full prod payload/JS/GPS evidence.
