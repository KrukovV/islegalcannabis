Goal: Keep one canonical `/new-map` MapLibre runtime across `/`, `/new-map`, and `/c/[code]`, with CountryPageData as the only SEO/map/card truth, stable nearby/social-reality enrichment, exact non-regressing map visuals, truthful local overlays, and a live local AI companion that stays honest about model quality.
State: checkpoint=.checkpoints/20260418-140026.patch; CI=PASS; Smoke=PASS
Done: Route/map/SEO truth remains locked on the unified `/new-map` stack. The AI companion remains single-model on `qwen2.5:1.5b` with no runtime fallback switching. Context is lightweight v2 with short history, location anchoring, deterministic retry/final guards, and deterministic engines for basic law, nearby, culture, compare, market access, trace/product/small-amount/travel risk. Nearby AI integration formats truth-engine output as 4 ranked places with effective km, access type, destination `Risk:`, and the mandatory border warning. Slang/greeting hard guard now runs before memory/context/LLM in the API route and before runtime fallback; greetings such as `Еу, как сам?` return deterministic `companion-engine`. Latest anti-nearby contract: greetings return exactly `Все норм 🙂`, dialog styling no longer appends CTA/follow-up offers, and nearby truth is called only for explicit `isNearSearch(query)` in the current user input. Live headed UI giga-QA `Austria/Chile/Senegal/Malaysia/Norway/Zambia/Estonia/Uruguay/Philippines/Georgia × 11` passed `110/110 strong`, `usable=0`, `weak=0`, `forcedNearbyFailures=0`, `slowTurns=0`, avg first `53ms`, avg total `1131ms`; screenshots are in `~/islegalcannabis_archive/anti-nearby-giga-ui-20260418-final/`, operational report is `Reports/anti-nearby-giga-ui-20260418-final.json`. Vercel build failure from `composeFollowUp(context)` stale arguments was fixed by switching remaining calls to `composeFollowUp()`. Local production build `npm -w apps/web run build` passes; targeted nearby/AI/dialog-style tests are `62 passed`; local lint is green.
Open questions: UNCONFIRMED `deepseek-r1:1.5b` remains installed but is still rejected for runtime promotion on this host, and `deepseek-r1:7b` remains intentionally uninstalled on `8 GB RAM`. Other locally installed models may still exist in Ollama tags for diagnostics, but the product runtime default is intentionally single-model.
