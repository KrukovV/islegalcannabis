Goal: Complete a 100% visual + data popup/wiki audit for all 300 map entities, keep popup and SEO content aligned with Wiki-backed sources, and remove resolver gaps that leave runtime popups without canonical wiki references.
State: checkpoint=.checkpoints/20260627-133808.patch; CI=PASS; Smoke=PASS
Done: Popup/SEO parity, AQ popup context, Washington canonical title alias merge, and prior prod/local card-index parity were already green. Hidden parent-covered territory popups stopped emitting blank wiki sources: `apps/web/src/new-map/countrySource.ts` resolves fallback wiki evidence in a fixed order of dedicated harvested `Cannabis in_*` article, parent-jurisdiction cannabis/legal Wikipedia source, then canonical territory wiki row from SSOT. Targeted harvest refreshed `GF`, `GP`, `MQ`, and `RE` to real dedicated cannabis pages; runtime snapshot resolves `BV,BQ,CX,CC,GF,GP,MQ,YT,RE,SJ,TK` to concrete Wikipedia URLs, and the targeted visual manifest no longer reports `NO_WIKI_URL` for that 11-geo territory set. This turn also added an explicit disputed-geo inheritance table for `BJN,BRT,KAS,PGA,SCR,SER,SPI`, fixed the broken `PGA -> Wake Island` label back to `Spratly Islands`, and wired requested synthetic/disputed geos into the opt-in popup visual audit without changing the default 300-row audit universe. Fresh runtime `/api/new-map/card-index` snapshot still has `307/307` entries with `missing wiki url = 0`; the remaining inherited-no-profile set is `63` geos and is expected because those entities currently reuse parent/claimant/territory Wikipedia evidence rather than a dedicated harvested `Cannabis in_*` profile. `node --test tools/knowledge/harvest_cannabis_knowledge.test.mjs` passes `59/59`, targeted `countryPageStorage` vitest is green at `31/31`, and local Playwright visual audits passed for both the 11 territory geos and the 7 disputed/synthetic geos on the singleton dev server.
Now: Coverage remains green for runtime wiki URLs (`307/307`, zero missing); the only unresolved surface is broader enrichment quality for the `63` inherited no-profile geos, while disputed fallback mapping, targeted audits, and the last recorded `bash tools/pass_cycle.sh` remain green with `Reports/ci-final.txt` showing `POST_CHECKS_OK=1` and `HUB_STAGE_REPORT_OK=1`.
Open questions: Default Playwright `webkit` launch remains workable for the targeted popup audit, but broader default-browser parity beyond these targeted cases is still UNCONFIRMED.
