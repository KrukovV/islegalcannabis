Goal: Improve `/new-map` PageSpeed/TBT without regressing map load, labels, hover, popup, colors, or GPS.
State: checkpoint=.checkpoints/20260616-004112.patch; CI=PASS; Smoke=PASS
Done: Baseline live PSI before this perf pass recorded `desktop score=60 mobile score=55`; moved the heavy `@/lib/countryCardEntry` builder off the initial `/new-map` route path and into an on-demand popup fallback import inside `apps/web/src/new-map/MapRoot.tsx`; rebuilt and confirmed `/new-map` entry JS dropped from 5 chunks / `1206152` bytes to 4 chunks / `1106172` bytes (`-99980 bytes`, `-97.6 KiB`); local `measure_new_map_js_city` stayed functionally green with first-party script transfer `1207.8 KiB -> 1197.1 KiB` (`-10.7 KiB`) and city-label time `747ms -> 364ms` (`-383ms`); local `measure_new_map_gps_flow` stayed green with `marker=24ms center=31ms recenter=11ms`
Now: UNCONFIRMED until final `pass_cycle`, prod deploy, and post-deploy live PSI verify whether the current `desktop=60/mobile=55` baseline improves without regressions.
Open questions: UNCONFIRMED whether the post-deploy live PSI delta is large enough after the lazy card-entry split alone or whether an additional initial-route JS reduction pass is still required.
