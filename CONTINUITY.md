Goal: finalize production bypass stability spec/docs and produce a stability tag for current by-pass + perf baseline.
State: checkpoint=.checkpoints/20260615-005400.patch; CI=PASS; Smoke=PASS
Done: `apps/web/src/app/layout.tsx` includes preconnect/preload; `apps/web/src/app/new-map/page.config.test.ts` and `tools/vercel_bypass.test.mjs` contract tests pass; `docs/OPS.md` + `docs/VERCEL_BYPASS.md` updated; local payload/network/JS/perf artifacts re-measured; moved `maplibre-gl` stylesheet import from `apps/web/src/app/globals.css` into `apps/web/src/new-map/MapRoot.tsx` to remove render-blocking CSS from global route entry; split `apps/web/src/new-map/MapRoot.tsx` map runtime initialization through dynamic `apps/web/src/new-map/mapRuntime.ts` import to defer maplibre JS/CSS until mount; completed local before/after JS payload delta collection and baseline comparison (`total 2350959→2172662`, `scripts 1420761→1242760`, `T7 4915→2932`, `long_tasks_count 8→9`, `first_party_chunk_unused_source 52565→743`, `first_party_estimated_unused_transfer 8919→111`); produced pass-key live probe evidence in `Reports/vercel-bypass-live/last_run.json` and `Reports/prod-live-gate/latest.json`; ran `bash tools/pass_cycle.sh` with `VERCEL_AUTOMATION_BYPASS_SECRET='mHHbgbRdY1TnZgmSnLBgZ7w5jVVAXC41'` and got green (`PROD_LIVE_OK=1`, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`).
Now: stage remaining map runtime artifact and push via `tools/commit_if_green.sh`.
Open questions: UNCONFIRMED Whether to force `PROD_EXTENDED_TAIL_GATES=1` on the next pass for full prod payload/JS/GPS evidence.
