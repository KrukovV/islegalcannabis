Goal: Update `/new-map` docs/spec to match the shipped basemap-ready/GPS behavior, confirm real-user QA from `2.mov`, and publish a stable tagged baseline on GitHub.
State: checkpoint=.checkpoints/20260615-235005.patch; CI=PASS; Smoke=PASS; docs/spec-only diff is green in the repo gate and ready to publish
Done: Reviewed `2.mov` from `/Users/james/Projects/isLegal/2.mov` at `0.5s` cadence into `~/islegalcannabis_archive/20260615-video2-analysis/`; confirmed the user-path looks healthy: world map visible by about `4.0-5.0s`, labels visible during zoom by about `12.0-17.5s`, Safari GPS prompt around `19.5s`, GPS accepted with marker visible around `21.0s`, and later pan/zoom continues with labels and no blocking app error UI through `40.95s`; updated the contract to define `data-map-ready="1"` as basemap-interactive readiness while `NM_T7_FIRST_FILL_RENDERED` remains the canonical legal-fill metric; updated ops docs with the archived `2.mov` human-QA evidence; updated versioning docs to reflect the actual `stability/*` tag track and the next patch tag `stability/0.0.10`; confirmed `bash tools/pass_cycle.sh` PASS on the docs/spec diff with `POST_CHECKS_OK=1` and `HUB_STAGE_REPORT_OK=1`
Now: Commit and push the docs/spec baseline with the `stability/0.0.10` tag.
Open questions: UNCONFIRMED whether any protected-domain headless prod gate will still emit Vercel challenge noise on future builds even when the real user path remains healthy.
