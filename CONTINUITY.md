Goal: Keep one canonical `/new-map` MapLibre runtime across `/`, `/new-map`, and `/c/[code]`, with CountryPageData as the only SEO/map/card truth, stable nearby/social-reality enrichment, exact non-regressing map visuals, truthful local overlays, and a live local AI companion that stays honest about model quality.
State: checkpoint=.checkpoints/20260417-114532.patch; CI=PASS; Smoke=PASS
Done: Route/map/SEO truth remains locked on the unified `/new-map` stack. The AI companion is now simplified to a single local model path: `qwen2.5:1.5b` is the only default runtime model, fallback-model defaults and benchmark defaults were removed, the UI now shows `модель не ответила, попробуй ещё раз` instead of switching models, and the system prompt was replaced with the new companion-core prompt. Provider timeouts were raised for this single-model path, local metrics/working-model data were reduced to `qwen2.5:1.5b`. Context handling is now the lightweight v2 path through `apps/web/src/ai-assistant/buildContext.ts`: short system prompt, explicit `Location`, short `Topic`, last two turns only, compare guard only when needed, and stricter memory filtering by active location. The determinism layer is also in: Ollama sampling is `temperature=0.7/top_p=0.85`, first-token timeout is now `40s`, total timeout is `90s`, output is normalized, bad/short/drift/generic generations get exactly one retry, and if the retry is still bad the existing truth-engine answer is used as the final output rather than showing an empty/fallback UI. `AIBar` keeps the idle-settle fix and now preserves usable streamed text if a short final chunk arrives; only genuinely short final output becomes `Уточни вопрос, я отвечу точнее`. Local lint is green, targeted AI tests are `27 passed`. Live `js_repl` Playwright runs now show the prior `Iran/Finland/Brazil/Cambodia × 6` pass (`24/24`, `pass90=true`, `emptyAnswers=0`, `fallbacks=0`, `drifts=0`, report `Reports/ai-jsrepl-ui-4x6.json`, screenshots `~/islegalcannabis_archive/jsrepl-ui-live-20260416-v13/`), the random-country legal pass after explicit server reset + warm (`Germany/Japan/Canada/Portugal/Thailand/Morocco × 6` is `36/36`, `strong=6`, `good=30`, `weak=0`, `driftTurns=0`, `avgResponseMs=26803`, `avgFirstTokenMs=3171`, report `Reports/ai-jsrepl-ui-random6-20260416.json`, screenshots `~/islegalcannabis_archive/jsrepl-ui-random6-20260416-v2/`), the mixed `5×6` country-plus-culture pass (`Reports/ai-jsrepl-ui-mixed5-20260416.json`, screenshots `~/islegalcannabis_archive/jsrepl-ui-mixed5-20260416-v3/`), and the improvised `3 countries × 7 non-template questions` rerun on `Italy/Nepal/UAE` (`Reports/ai-jsrepl-ui-improv3-20260416.json`, screenshots `~/islegalcannabis_archive/jsrepl-ui-improv3-20260416-v2/`). The last recurring weak class from those live runs, `CBD/oil/gummies/flower`, is now routed through a minimal deterministic `product-risk-engine` instead of ambiguous LLM generation. A live rerun of the previously failing Italy prompt `If I carry CBD flower, is that still risky?` now returns a grounded answer in `5761ms` total with `2685ms` first token; screenshot is archived at `~/islegalcannabis_archive/jsrepl-ui-cbd-fix-20260416-v1/italy-cbd-flower.jpg`. A new live stress run on two fresh countries, `Chile/Malaysia × 15`, exposed three weak turns: compare after a product thread, compare on a route-selected country, and a severe-law `tiny amount` question (report `Reports/ai-jsrepl-ui-stress2x15-20260416.json`, screenshots `~/islegalcannabis_archive/jsrepl-ui-stress2x15-20260416-v1/`). Those are now closed through simplification rather than more prompt weight: compare questions no longer inherit prior product subtopics, compare now respects the selected route country as `primary`, compare answers are streamed from a deterministic `compare-engine`, and `tiny amount / serious situation` questions go through a deterministic `risk-engine`. Local lint is green again and targeted AI tests are now `30 passed`. The latest deploy blocker from Vercel is closed too: `aiRuntime.ts` no longer contains the unreachable `nearby` branch that broke TypeScript narrowing, `buildContext.ts` now returns a typed `LlmMessage[]`, local `next build` is green, and the repo is ready for a fresh push-triggered deploy. Live js_repl reruns confirm the fixes: `Malaysia tiny amount` now answers in `1261ms` total / `451ms` first token (`~/islegalcannabis_archive/jsrepl-ui-stress2x15-20260416-v2/mys-q1-fixed.jpg`), and the previously weak compare turns now stay grounded to the selected country pair with deterministic output (`Reports/ai-jsrepl-ui-stress2x15-fixes-compare-20260416-v2.json`, screenshots `~/islegalcannabis_archive/jsrepl-ui-stress2x15-20260416-v4/`). A further live improvised run on three new countries, `Austria/Kenya/South Korea × 8`, is now captured in `Reports/ai-jsrepl-ui-improv3x8-20260416.json` with screenshots in `~/islegalcannabis_archive/jsrepl-ui-improv3x8-20260416-v3/`: all `24/24` turns completed, average first token stayed very fast (`Austria 116ms`, `Kenya 205ms`, `South Korea 107ms`), and the only weak answer was not a context drift but a repeated deterministic trace-risk reply on Austria. That weak class is now split into separate `smell` versus `grinder/residue` wording inside the deterministic `trace-risk-engine`; rerun evidence is stored in `Reports/ai-jsrepl-ui-improv3x8-aut-rerun2-20260416.json` with screenshots under `~/islegalcannabis_archive/jsrepl-ui-improv3x8-20260416-v5/`. A new live six-country run on `France/Turkey/Peru/Nigeria/Czechia/New Zealand × 8` is captured in `Reports/ai-jsrepl-ui-random6x8-20260416.json` with screenshots in `~/islegalcannabis_archive/jsrepl-ui-random6x8-20260416-v1/`. The run completed all `48/48` turns; the only weak class appeared late on `New Zealand q4-q6`, where tourist/public, foreign-prescription, and forgotten-bag prompts drifted into generic safety prose or a too-short clarify answer. That bottleneck is now closed through a deterministic `travel-risk-engine` for those exact travel-risk patterns, and the targeted live rerun is stored in `Reports/ai-jsrepl-ui-random6x8-nzl-rerun-20260416.json` with screenshots under `~/islegalcannabis_archive/jsrepl-ui-random6x8-20260416-v2/`.
Open questions: UNCONFIRMED `deepseek-r1:1.5b` remains installed but is still rejected for runtime promotion on this host, and `deepseek-r1:7b` remains intentionally uninstalled on `8 GB RAM`. Other locally installed models may still exist in Ollama tags for diagnostics, but the product runtime default is now intentionally single-model.
