Goal: Reproduce and fix the production GPS failure shown in `1.mov`, remove/justify legacy JavaScript warning, update project specs/docs, and verify locally plus cold prod without regressions.
State: Video evidence confirms production GPS failure path: Safari asks for geolocation permission, user clicks Allow, but the map stays on the old France/Europe marker instead of moving to fresh GPS.
Done: Extracted `1.mov`; prod stale-GPS baseline failed before fix (`STALE_GPS_NOT_REFRESHED`). Implemented GPS refresh-on-click, stale-GPS gate, local WebKit GPS E2E PASS 3/3, final-build local cold GPS/hover/Zoom PASS with `PROD_GPS_OK=1`, `stale_saved_gps_refreshed=1`, `gps_marker_ms=3`, `gps_center_ms=4`, `zoom_in_city_labels=42`, `zoom_out_rendered_countries=216`, console/page errors `0/0`.
Done: Removed Next module polyfill bundle from modern build via alias; exact production chunk grep has no Next polyfill-module patterns; final-build local JS report `legacy_kib=0`, `legacy_signals=0`, city-label latency `252 ms`; `node tools/source_maps_build.test.mjs` PASS.
Now: Commit/push/deploy, wait for production alias, then run mandatory cold prod gates and final `pass_cycle`.
Open questions: Production post-deploy metrics are pending.
