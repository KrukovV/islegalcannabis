Goal: Complete a 100% visual + data popup/wiki audit for all 300 map entities, generate popup/wiki screenshot pairs and per-entity audit rows, and fix the shared extractor/mapper so History/Culture/Cultivation/Market/Enforcement only show relevant wiki-backed content without boilerplate bleed.
State: checkpoint=.checkpoints/20260627-010627.patch; CI=PASS; Smoke=PASS
Done: The shared extractor is no longer flat-regex-only. `tools/knowledge/harvest_cannabis_knowledge.mjs` now has heading-aware `legal_status` and `legal_reform` contexts, blocks legal/product boilerplate from `Products`, keeps pure legal status out of `Enforcement`, stops reform/policy text from leaking into `Market`, normalizes `is illegal, but ...` canary sentences before they are written into semantic sections, drops broken `known as.` fragments, keeps derived local names out of `Products`, routes legalization/history sentences back out of `Market`, routes plain `Enforcement` headings into `enforcement_notes`, keeps tourism arrest/court copy out of `Market`, keeps Belgian legal-grey policy copy out of `Cultivation`/`Market`, lifts trade-dominant exporter facts from `History` into `Market`, keeps lead enforcement wording such as `enforced opportunistically` in `Enforcement`, keeps legal-status history rows such as Central African Republic out of `Cultivation`, keeps ordinance/referendum vote copy in `History` instead of `Market`, treats cultivation headings that also mention laws as `cultivation`, keeps troop-consumption ban history out of `Market`, allows lead `drug control` status text to seed `Enforcement`, preserves `Legality of hemp` under a cultivation parent as `cultivation`, blocks legal-popularity boilerplate from inflating `Culture`, routes `History > ... Ordinance/Act/Law` and `History > ... sale restricted / restriction / prohibition / reform` subheadings back to `history`, drops local-name glossary sentences out of `Culture`/`Products`, keeps policy-opinion legalization sentences in `History`, keeps patient/program growth copy out of `Cultivation`, keeps legal-product taxonomy such as CBD/derivative classification out of `Products`, treats `permit/permitted/not permitted` as legal-status language, routes medicinal-use policy and legalization-effort copy out of `Traditional Use` into `History`, routes `widely available despite illegal status` availability copy out of `Culture`, routes transit/smuggling text out of `Traditional Use` into `Market`, routes `illegal, but widely produced/consumed` market copy out of `Culture`, routes legal-grey `cannabis clubs` copy from neutral/culture/legal contexts into `Market`, keeps comparative foreign-law cultivation copy out of `Cultivation`, blocks `hemp/hashish` law-or-cultivation false positives from surfacing in `Products`, keeps present-day `as of YEAR` legal-status boilerplate out of `History`, keeps `legal + widely used drugs` lead boilerplate out of `Culture`, fixes Washington state title aliasing so `Cannabis in Washington (state)` replaces stale generic `Cannabis in Washington` cache content, canonicalizes alias titles inside `sameKnowledgeSource()` so stale generic/state title variants are replaced instead of merged, and normalizes popup section labels by stripping any middle-dot page suffix instead of only `· Wikipedia:`. Added/kept regressions for Japan legal-status/reform leakage, local cache title inference, canary boilerplate normalization, derived-local-name product inflation, market-history leakage, Belgium legal-reform boilerplate, Belize tourism enforcement leakage, Bhutan enforcement headings, history trade facts, lead enforcement wording, Central African Republic history classification, history vote-copy classification, Ecuador cultivation-law headings, Egypt history-vs-market consumption copy, Eswatini lead drug-control enforcement seeding, Fiji hemp-legality cultivation routing, France/Namibia/Nigeria/Uruguay legal-popularity filtering, Guyana hemp-ordinance routing, glossary filtering, policy-opinion routing, patient-growth cultivation filtering, legal-product boilerplate filtering, CBD-pharmaceutical exception filtering, North Dakota/North Korea `Products` false-positive filtering, Palau illegal-production market routing, medicinal-use reform-copy classification, `cannabis clubs` market classification, comparative foreign-law filtering, history sale-restriction classification, Vanuatu industrial-hemp law-change product filtering, Venezuela lead-penalty traditional-use filtering, Washington alias merge replacement, availability-under-illegality culture filtering, and Macau smuggling-route classification. Fixed `buildLocalCannabisCacheIndex()` so cached cannabis articles whose lead uses plain-text country names are discoverable offline. Added env-selectable Playwright browser config plus a full-page screenshot fallback that stitches large wiki pages into a single PNG via `sharp`. Added `apps/web/scripts/popup-visual-audit.ts` plus `npm run popup:visual:audit`, which bypasses the flaky Playwright test-runner loop and captures deterministic popup/wiki screenshot pairs in raw Playwright. Added `apps/web/scripts/popup-visual-audit-full.ts` plus `npm run popup:visual:audit:full`, which checkpoints each alphabetical batch into `Artifacts/popup-visual-audit/batches/` during the run and then merges them into one `300-row` manifest/report so the full audit is resumable instead of depending on a single fragile long session. Changed `/api/new-map/card-index` to return a fresh runtime snapshot with `no-store` caching so popup audits stop reading stale `cannabisProfile` data after targeted harvest refreshes, reworked `apps/web/src/lib/cannabisProfile.ts` so dev-time JSON reload stays server-only and does not pull `node:fs` into the client bundle, widened the popup audit `marketSignalRe` so legitimate `retailers` / `importation` / `imported` / `export` / `exporting` / `cannabis clubs` / `private collective` market copy is not misflagged as boilerplate, added a retry on the first failed popup selection before emitting `POPUP_NOT_VISIBLE`, added a retry path when `setSelectedGeo()` hits `Execution context was destroyed`, and switched the wiki-side comparator from headings-only to substantive section text. Re-ran targeted harvest refreshes for all earlier flagged canary/batch geos through `270-279`, then refreshed `GT`, `JP`, `UY,VU,VE`, `US-WA,US-WV,US-WI`, and `US-WA` again after the latest alias-merge fix; the resulting `knowledge_db.json`, `first_wave_profiles.json`, and `local_names.dictionary.json` reflect the cleaned sections. Re-ran popup/wiki visual audit on `CA,CU,JP,MM,PG,VN`; and on alphabetical batches `0-9`, `10-19`, `20-29`, `30-39`, `40-49`, `50-59`, `60-69`, `70-79`, `80-89`, `90-99`, `100-109`, `110-119`, `120-129`, `130-139`, `140-149`, `150-159`, `160-169`, `170-179`, `180-189`, `190-199`, `200-209`, `210-219`, `220-229`, `230-239`, `240-249`, `250-259`, `260-269`, `270-279`, `280-289`, and `290-299` via standalone Chromium audit. Then ran the new full aggregator, which completed `300/300` and produced a merged `Artifacts/popup-visual-audit/manifest.json` plus `report.csv` with `total=300`, `datasetTotal=300`, `popupCaptured=300`, `wikiCaptured=289`, and `badCount=0`. The full manifest now proves `NO_RUNTIME_CANNABIS_PROFILE=60`, `NO_WIKI_URL=11`, and resolves `LY` as a captured row with `NO_RUNTIME_CANNABIS_PROFILE`. `node --test tools/knowledge/harvest_cannabis_knowledge.test.mjs` now passes `58/58`. `npm -w apps/web run popup:profile:audit && node tools/gates/popup_profile_audit_gate.mjs` now passes with `processed=300/300`, `template_sections=0`, `source_errors=0`, `status_mismatches=0`, and `color_mismatches=0`. Local dev was confirmed live on `http://127.0.0.1:3000/wiki-truth` under the singleton rule, `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000` visual audit for `US-WA` passed on that live server, and the runtime `/api/new-map/card-index` row for `US-WA` now resolves to `Cannabis in Washington (state)` with clean section mapping and no audit mismatches. After the explicit cleanup request, all heavyweight generated files under `Artifacts/` were deleted; the repo now keeps only the compact audit summaries `Artifacts/popup-visual-audit/manifest.json` and `Artifacts/popup-visual-audit/report.csv`, and `node tools/gates/log_size_guard.mjs` is green again. `apps/web` now builds cleanly after fixing the typed section-map fallback in `scripts/popup-profile-audit.ts`, the nullable heading extraction in `scripts/popup-visual-audit.ts`, and the `buildCannabisProfileCard()` section-id typing. Golden commit `0508900d1e6bcd76537fa01935f5d31ea9acbf2e` is on `origin/main`, tagged as `prod/20260627-popup-audit-golden`, and prod `/api/build-meta` resolves to that commit. Vercel deployment protection is confirmed via the cookie-seeding path (`method2_api_cookie_seed`) with live proof in `Reports/vercel-bypass-live/last_run.json`. Local vs prod popup/card-index parity on a 70-geo random sample is `70/70` with `datasetEqual: true` in `Reports/prod-local-popup-parity/2026-06-26T21-26-46-650Z/summary.json`. `tools/guards/run_all.mjs` no longer invokes `compact_ci_summary.mjs` as a guard, `compact_ci_summary.mjs` now exits immediately on interactive stdin, and `tools/measure_new_map_gps_flow.mjs` now waits for persisted center recovery after reload and retries hover acquisition until the map reports `hoveredId=FR`. The GPS runner fix was re-verified against prod twice in a row with `PROD_GPS_OK=1`.
Now: Prod-tagged golden commit `11c8981565ac6fbcf88727fbfc5158f07f4eb353` carries the guard runner, GPS runner, nearbyTruth threshold fix, and refreshed SSOT timestamps; current `origin/main` head `a77c4f89a573c26ade2b12a96cde96fa46e034d2` is the continuity-sync follow-up on top of that green state.
Open questions: The configured default `webkit` Playwright project still aborts at launch on this machine, so local audit proof currently depends on the standalone Chromium audit rather than the default browser target. In-app browser bootstrap is still UNCONFIRMED because it failed before connection on `sandboxCwd must be an absolute file URI`.
