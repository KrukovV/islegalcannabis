Goal: Keep one canonical `/new-map` MapLibre runtime across `/`, `/new-map`, and `/c/[code]`, with CountryPageData as the only SEO/map/card truth, stable nearby/social-reality enrichment, exact non-regressing map visuals, truthful local overlays, and correct live “Where am I” behavior on SEO routes.
State: checkpoint=.checkpoints/20260413-182016.patch; CI=PASS; Smoke=PASS
Done: CountryPageData now drives `/c/[code]` SSR, `/api/new-map/card-index`, `/api/new-map/countries`, and the SEO overlay panel with graph links/citations. The latest production build is `origin/main=a6a8b0e7028b3322b7e2481bd928912d4f39d4a9`, and `https://islegal.info/api/build-meta` reports the same commit with `builtAt=2026-04-13T13:38:45.412Z`. Popup/card output now carries `mapReason` for all non-trivial yellow and green buckets, the frozen pastel palette stays locked to exact HEX values, and timeline-aware current-law parsing now prefers the latest `isCurrent` law text so `DMA -> DECRIM`, `GEO -> DECRIM`, `ESP -> MIXED`, and `IRN -> ILLEGAL` stay stable. The route-side large SEO panel now renders from the same `CountryCardEntry.panel` path as the small anchored popup, so route overlays and click-popups no longer diverge on AUS-like yellow/restricted cases; the same headed-pass now confirms `/c/usa` and `/c/us-ca` both keep the route `i` marker wired to the large SEO panel, while `/c/us-ca` still opens the small anchored popup for clicked state geos. SEO now keeps one canonical root sitemap source: `/sitemap.xml` is generated only from `app/sitemap.ts` as a single XML `urlset` with `/` plus all canonical `/c/*` pages, using CountryPageData `updated_at -> lastmod` with build-time fallback only. Production `https://islegal.info/sitemap.xml` now returns that single `urlset` with `Content-Type: application/xml`, which removes the old root-sitemap source conflict for GSC. `/c/[code]` metadata still emits hreflang alternates for `en/es/fr/de`, and `/es|fr|de/c/[code]` SSR routes still exist with canonical back to `/c/[code]`. Local result UX now renders a structured source-linked `StatusPanel`: `buildResultViewModel()` groups `critical`, `info`, and `why` reasons from existing `legal_ssot` and facts only, `CountrySeoPage` exposes stable law anchors, and `ResultCard` links every displayed reason either to `/c/[code]#...` or a verified source URL. The status panel is now the only status-render path inside `ResultCard`; old `whyBullets`, generic sources list, and inline badge/status duplication are gone, while the map-side `seoOverlayPanel` is replaced by one `UnifiedSeoStatusPanel` that shows status, summary, intent, related links, and sources in a single overlay. New-map country popups are now source-linked infographic cards instead of raw text dumps: each popup renders one badge, one summary line, critical/info/why blocks, direct source links, and a `/c/[code]` CTA. Result UI is now SSOT-locked too: `ResultViewModel` derives visual level from `legal_ssot.result_status`, `StatusPanel` throws on missing SSOT, and `ResultCard` no longer falls back to `profile.status_*` or raw medical/recreational fields. The map/chat layout is now coordinated by one bottom safe area so the unified panel scrolls above the fixed chat dock without overlap; AI chat can collapse to a single Ask button and reopen without losing in-memory state; reason links resolve without dead duplicate source targets; and `COUNTRY_PAGE_HASH_MISMATCH` no longer leaks into visible dev UI or Flight payload text. Click popups remain the small per-country panel path, are again viewport-clamped around the selected country instead of center-screen, and `/c/[code]` route geos now use the same popup path through a client-safe card-entry helper rather than a server-only import. Country legal layer ownership is now explicit in storage too: `CountryPageData.sources.legal` resolves only to `Cannabis_in_*`, country citations now lead with that legal article instead of `/wiki/<Country>`, popup/SEO `Legal source →` no longer self-links to `/c/[code]`, and intent copy no longer leaks `normalized source data` / `normalized model` wording into the live overlay.
Now: Local dev keeps the debug/runtime cards, truthful build watcher, full AI bar, Antarctica ASCII overlay, anchored infographic country popups on `/new-map`, and one shared map pipeline for `/`, `/new-map`, `/c/[code]`, and `/es|fr|de/c/[code]`. `/c/[code]` again shows the large SEO panel for the route geo and toggles it only from the route `i` marker, while ordinary country clicks keep the small anchored status popup path; the large panel now shares the exact same level title, summary, critical/info/why reasons, and law snapshot summaries as the small popup; production confirms the same split UX on `3dba263557b2bdd3e3000168ee12a98b4b6dd85a`. The blue “Where am I” marker is owned only by live IP/GPS state, and the SEO `i` marker stays bound only to the route geo instead of all sibling states. The local ASCII runtime starts after `5s` in dev, auto-rotates through the registry after each scenario completes, and production keeps the long delayed-start behavior plus the hidden runtime/debug overlays and disabled AI input. Local SEO storage/index/graph include `us-*` nodes, state route pages use `geo_code`, and country truth carries raw-vs-final medical status plus single-pass rule-table `distribution` derivation for the map, popup, and `/api/check` consumers. Derived legal parsing now consumes cached traversal text plus live Wikipedia API plaintext extracts for canonical main-article titles during storage generation, while keeping summary/table truth authoritative for country-level recreational and medical statuses and softening only final risk/legal resolution when enforcement is weak. `/c/[code]` now server-renders the route summary card plus five intent H2 sections from storage truth, and the derived debug layer distinguishes `no_secondary_source` from `no_prison_signal` instead of treating both as parser uncertainty. Intent extraction is now deterministic from `notes_normalized`, `legal_model`, `facts`, and `signals`: `/c/[code]` sorts the five SSR H2 blocks by intent strength, ranks per-intent related regions by `intent_strength × geo_graph_weight`, supports optional server-side `?q=` intent boosting, and throws `COUNTRY_INTENT_INVALID` on legal-model contradictions instead of silently drifting. New-map status rendering still keeps the explicit truth/view split and the frozen reference palette. `resultStatus.ts` now uses a single deterministic formula with final enum `LEGAL | MIXED | DECRIM | ILLEGAL | UNKNOWN`: baseline comes from wiki recreational table truth, softeners come only from personal-use/fine/tolerance signals in primary country text, strong negatives stay on death/long-prison/strict-enforcement, `USA` remains a deterministic `MIXED` branch, and map snapshot generation derives `mapCategory` from the explicit view-layer rules instead of a second truth path. Popup/card output now carries `mapReason` so yellow countries with formal illegality explicitly explain the yellow bucket instead of looking arbitrary, while colors remain locked to the exact pastel contract (`#cde7cf`, `#f4e9c2`, `#ead0d1`, `#d7dcdc` plus the fixed hover set). Countries without SSOT status are no longer painted via fallback; the server filters and logs them explicitly (`NEW_MAP_FILTERED_MISSING_STATUS`) instead of silently coloring them yellow. The `/c/[code]` top header overlay is now removed completely from SSR: the old `hero/panel` shell no longer renders above the map, while the single H1 and legal summary remain in normal DOM flow inside `#seo-content`. The local AI assistant is now a grounded companion instead of a thin template wrapper: `aiRuntime.ts` builds explicit context from `CountryPageData`, derived legal signals, notes, social reality, travel risk, culture chunks, and dialog memory, then generates the answer from that context or hands the same grounded context to the local model. Production still keeps the AI input disabled, but local dev now supports state geos like `US-CA` in the AI route and grounded airport/travel follow-ups without inventing facts. Local runtime refresh now uses one full runtime signature across badge and update-banner: `/new-map` SSR emits `runtimeMode/mapRuntime/expectedOrigin` on the hidden runtime node, `BuildWatcher` compares the same full signature as `RuntimeParityBadge`, and `Обновить` can now surface any real `No Actual` mismatch and converge the hidden/visible runtime stamp to `/api/build-meta` after the hard-reload path. Timeline-aware current-law parsing is now in the derived SSOT path too: `CountryPageData` exposes `legal_timeline[]`, `extractLegalTimeline()` pulls dated law/policy/enforcement snippets from notes/facts/signal titles/citations, and `deriveResultStatusFromCountryPageData()` now prefers the latest `isCurrent` text over mixed historical article noise when building flags. The current-law regressions are now pinned for `DMA -> DECRIM`, `GEO -> DECRIM`, `ESP -> MIXED`, and `IRN -> ILLEGAL`, while `BE` now settles at `DECRIM` under the stricter current-law modifier path instead of drifting back to `MIXED`. SEO now runs through one canonical root sitemap contract from one SSOT: `/sitemap.xml` is generated by `app/sitemap.ts`, emits a single XML `urlset`, includes the homepage plus canonical `/c/*` pages only, and keeps truthful `lastmod` from CountryPageData `updated_at` with build-time fallback. Canonical `/c/*` pages still emit hreflang alternates to the locale routes; the old root `/sitemap.xml` route handler has been removed. The live legal source path is now strict for countries: route and click panels both show a colored status badge from the same `CountryCardEntry.panel` contract, `sources.citations[0]` is now the canonical `Cannabis_in_*` page, and `Legal source →` opens that external layer-2 article instead of looping back to the same route.
Open questions: UNCONFIRMED strict `ΔRGB ≤ 2` on the Germany sample only; water, AU, RU, and India matched reference values directly. UNCONFIRMED automated pixel-readback visibility for the delayed-start Antarctica ASCII path in Chromium (`asciiState="running"` but `nonEmptyPixels=0` in the automated probe). UNCONFIRMED local/wiki-only prison evidence for live `TT`: generic parser supports prison-bearing traversal docs, but the current cached Wikipedia lead/main-article extract for Trinidad and Tobago does not itself contain a prison term.
