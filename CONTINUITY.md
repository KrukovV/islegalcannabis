Goal: Complete a 100% visual + data popup/wiki audit for all 307 map entities, keep popup and SEO content aligned with Wiki-backed sources, and remove resolver gaps that leave runtime popups without canonical wiki references.
State: checkpoint=.checkpoints/20260628-025437.patch; CI=PASS; Smoke=PASS
Done: Deployed commit `2d64f30` to `main` via `commit_if_green.sh` (`good/20260627-165505`, `prod/stable`). `ViewportCountryPopup` compaction remains fixed generically. Wiki section detection no longer depends only on heading names: semantic classification now recognizes real content sections such as `Background`, `Developments`, `Early history`, and `Modern accounts`, while skipping non-content blocks like `References` and `See also`. A second general resolver/extractor fix is now in place: ambiguous generic cannabis titles such as `Cannabis in Georgia` are canonicalized to the specific country/state page when upstream sources prove it, dedicated articles with rich lead text but weak heading structure can still yield semantic wiki sections from lead paragraphs, and popup cleanup drops unattributed quote fragments. This removed false sparse classifications on real dedicated Wiki pages such as `AM`, `KP`, `GE`, `US-GA`, and `AR`, and removed the bad Sweden quote fragment from runtime popup content. Full popup/wiki audit was rerun after that fix and completed at `307/307`; heavy screenshot/html payload is archived at `/Users/james/islegalcannabis_archive/popup-visual-audit-full-20260628-023718`, while repo keeps fresh `Artifacts/popup-visual-audit/full-manifest.json`, `full-report.csv`, `full-summary.json`, `full-index.html`, `full-validation.json`, and `sparse-wiki-text-report.md`. The legacy 300-row `popup-profile-audit` now uses the same canonical cannabis source resolver, so `GE` and `US-GA` no longer fail against the generic disambiguation title; current audit is `processed=300/300`, `source_errors=0`. The full guard still enforces screenshot pairs, manifest freshness, section-comparison schema, no raw URLs, no repeated text across semantic sections, no fake thematic sections for root/no-page cases, and no section-count regressions for the baseline 300-row set.
Now: Full-run evidence is current: `datasetTotal=307`, `processed_geo_count=307`, `popupCaptured=307`, `wikiCaptured=307`, `rows_with_regressions=0`, `rows_with_source_trace_errors=0`, `rows_with_missing_artifacts=0`, `ui_compaction_fixed=25/25`, `conflicted=0`. Coverage summary from `Artifacts/popup-visual-audit/full-summary.json`: `individual_article=259`, `substantive_article=29`, `redirect_parent=9`, `no_individual_wiki_page=8`, `root_only=2`; sparse summary: `enriched=295`, `honestly_low_coverage=30`, `no_page=8`, `synthetic=0`, `failed=0`. Remaining standalone low-coverage-with-page count is `22`: `AX`, `BJN`, `BRT`, `BV`, `BQ`, `TF`, `HM`, `YT`, `OM`, `PS`, `SH`, `MF`, `SCR`, `SER`, `KAS`, `GS`, `SPI`, `PGA`, `SJ`, `TK`, `VA`, `WF`. Remaining no-page count is `8`: `GN`, `GW`, `LY`, `NR`, `NI`, `NE`, `SS`, `UM`. The human-readable sparse evidence report is `Artifacts/popup-visual-audit/sparse-wiki-text-report.md`; it includes the captured wiki text for every remaining sparse/no-page GEO.
Open questions: UNCONFIRMED
