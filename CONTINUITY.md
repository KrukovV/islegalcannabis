Goal: Задеплоить оптимизацию `/new-map` на prod и снять optimized prod delta с реальными скриншотами и замерами.
State: checkpoint=.checkpoints/20260530-203722.patch; CI=PASS; Smoke=PASS; perf_optimization=PASS; prod_deploy=IN_PROGRESS
Done: Unified `/new-map` MapLibre runtime is published for `/`, `/new-map`, and `/c/[code]`; stable production map runtime recorded as `6485d35`, docs deploy as `08e1430`. Production serves immutable hashed countries payload, keeps one MapLibre canvas, keeps AI input closed on prod, and has no supplemental Antarctica layer/source. Status Engine Audit v1 exists as review-only evaluator/reporting and keeps SSOT/map colors unchanged. Docs/spec refresh updated current `/new-map`, `/wiki-truth`, SSOT diff, network truth, UI singleton, storage hygiene, and Status Engine Audit v1 contracts. Vercel bypass update changed runbook/mobile docs to run user-supplied Method 1 (`extraHTTPHeaders`) and Method 2 (`context.request.get` cookie seed) before alternate variants, added `tools/vercel_bypass_live_probe.mjs`, updated `tools/vercel_bypass.test.mjs`, and kept `tools/measure_new_map_startup.mjs` on header cookie seed. Live prod with user-authorized secret: first run showed baseline `Vercel Security Checkpoint`/Code 21 while Method 1 and Method 2 both passed with `ok=1`, `access_block=0`, `title=Is cannabis legal?`, `root=1`, `ready=1`, `canvas=1`; repeated run also passed both methods and wrote screenshots/report to `Reports/vercel-bypass-live/`. Added mandatory prod live quality gate: `tools/prod_live_quality_gate.mjs`, `data/baselines/prod_live_quality_baseline.json`, Method 1/2 screenshot+timing enforcement in `tools/pass_cycle.sh`, docs in README/OPS/CONTRACT/DEV/REPO_POLICY, and unit tests. Focused prod gate with user-authorized secret passed: Method 1 `elapsed_ms=4409`, `map_ready_ms=3697`, screenshot `247239` bytes; Method 2 `elapsed_ms=3038`, `map_ready_ms=2461`, screenshot `248478` bytes; `PROD_LIVE_DEGRADATION=PASS`. Final `VERCEL_AUTOMATION_BYPASS_SECRET=<env> bash tools/pass_cycle.sh` passed with checkpoint `.checkpoints/20260530-200011.patch`, `CI_STATUS=PASS`, `Smoke 11/0 (total 12)`, `PROD_LIVE_OK=1`, Method 1 `elapsed_ms=3291`, `map_ready_ms=2723`, screenshot `247239` bytes, Method 2 `elapsed_ms=2566`, `map_ready_ms=1992`, screenshot `248478` bytes, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, and `PASS_CYCLE_EXIT rc=0`. New-map payload optimization focused test passed and `npm -w apps/web run build` passed after adding smaller precompressed countries payload, lazy card-index loading, and lazy US-states loading. Local prod-like after-measurement on `127.0.0.1:4010/new-map`: total `1201.3 KiB` (`-4058.9`), countries `521.5 KiB` (`-3465.3`, `br`), card-index `0 KiB`, us-states `0 KiB`, long tasks `5` (`-6`), first fill `3933 ms` (`-6822`), screenshot `Reports/new-map-payload/local-prod-after.chromium.png`. Prod payload gate passed on current deploy: total `2152.2 KiB`, countries `1471.1 KiB` (`br`), card-index `40.8 KiB`, us-states `0 KiB`, `PROD_PAYLOAD_OK=1`, report `Reports/new-map-payload/prod-gate-1780162434829.chromium.json`. Final pass_cycle passed with checkpoint `.checkpoints/20260530-203722.patch`, `PROD_LIVE_OK=1`, `PROD_PAYLOAD_OK=1`, prod payload total `2152.2 KiB`, countries `1471.1 KiB`, long tasks `2`, first fill `5875 ms`, screenshot `Reports/new-map-payload/prod-gate-20260530T173709Z-5512.chromium.png`, `POST_CHECKS_OK=1`, `HUB_STAGE_REPORT_OK=1`, and `PASS_CYCLE_EXIT rc=0`. Secret scan across docs/tools/data/Reports/CONTINUITY/.checkpoints found no token. Physical iPhone/Android smoke remains UNCONFIRMED on this host.
Now: Preparing guarded commit/push deploy path and post-deploy prod payload measurement.
Open questions: UNCONFIRMED full post-deploy browser matrix after docs deploy is constrained by Vercel Security Checkpoint automation/bot mitigation. UNCONFIRMED `/c/<code>` local browser route opening for the Status Engine Audit v1 wave because UI_SINGLETON_RULE prevented starting another dev UI while `.next/dev/lock` existed and no local HTTP UI was reachable.
