Goal: Keep one canonical `/new-map` MapLibre runtime across `/`, `/new-map`, and `/c/[code]`, with CountryPageData as the only SEO/map/card truth, stable nearby/social-reality enrichment, exact non-regressing map visuals, truthful local overlays, and correct live “Where am I” behavior on SEO routes.
State: checkpoint=.checkpoints/20260416-013813.patch; CI=PASS; Smoke=PASS
Done: CountryPageData now drives `/c/[code]` SSR, `/api/new-map/card-index`, `/api/new-map/countries`, and the SEO overlay panel with graph links/citations. The latest production build is `origin/main=00dae7204a2f22876de58265726c1ff103998f00`, and `https://islegal.info/api/build-meta` reports the same commit with `builtAt=2026-04-13T15:26:14.013Z`. Popup/card output now carries `mapReason` for all non-trivial yellow and green buckets, the frozen pastel palette stays locked to exact HEX values, and timeline-aware current-law parsing now prefers the latest `isCurrent` law text so `DMA -> DECRIM`, `GEO -> DECRIM`, `ESP -> MIXED`, and `IRN -> ILLEGAL` stay stable. The route-side large SEO panel now renders from the same `CountryCardEntry.panel` path as the small anchored popup, so route overlays and click-popups no longer diverge on AUS-like yellow/restricted cases; the same headed-pass now confirms `/c/usa` and `/c/us-ca` both keep the route `i` marker wired to the large SEO panel, while `/c/us-ca` still opens the small anchored popup for clicked state geos. SEO now keeps one canonical root sitemap source: `/sitemap.xml` is generated only from `app/sitemap.ts` as a single XML `urlset` with `/` plus all canonical `/c/*` pages, using CountryPageData `updated_at -> lastmod` with build-time fallback only. Production `https://islegal.info/sitemap.xml` now returns that single `urlset` with `Content-Type: application/xml`, which removes the old root-sitemap source conflict for GSC. `/c/[code]` metadata still emits hreflang alternates, and `/[lang]/c/[code]` SSR routes still exist. Local result UX now renders a structured source-linked `StatusPanel`: `buildResultViewModel()` groups `critical`, `info`, and `why` reasons from existing `legal_ssot` and facts only, `CountrySeoPage` exposes stable law anchors, and `ResultCard` links every displayed reason either to `/c/[code]#...` or a verified source URL. The status panel is now the only status-render path inside `ResultCard`; old `whyBullets`, generic sources list, and inline badge/status duplication are gone, while the map-side `seoOverlayPanel` is replaced by one `UnifiedSeoStatusPanel` that shows status, summary, intent, related links, and sources in a single overlay. New-map country popups are now source-linked infographic cards instead of raw text dumps: each popup renders one badge, one summary line, critical/info/why blocks, direct source links, and a `/c/[code]` CTA. Result UI is now SSOT-locked too: `ResultViewModel` derives visual level from `legal_ssot.result_status`, `StatusPanel` throws on missing SSOT, and `ResultCard` no longer falls back to `profile.status_*` or raw medical/recreational fields. The map/chat layout is now coordinated by one bottom safe area so the unified panel scrolls above the fixed chat dock without overlap; AI chat can collapse to a single Ask button and reopen without losing in-memory state; reason links resolve without dead duplicate source targets; and `COUNTRY_PAGE_HASH_MISMATCH` no longer leaks into visible dev UI or Flight payload text. Click popups remain the small per-country panel path, are again viewport-clamped around the selected country instead of center-screen, and `/c/[code]` route geos now use the same popup path through a client-safe card-entry helper rather than a server-only import. Country legal layer ownership is now explicit in storage too: `CountryPageData.sources.legal` resolves only to `Cannabis_in_*`, country citations now lead with that legal article instead of `/wiki/<Country>`, popup/SEO `Legal source →` no longer self-links to `/c/[code]`, and intent copy no longer leaks `normalized source data` / `normalized model` wording into the live overlay. US states now have the same two-layer legal source contract: the primary table layer still comes from `Legality_of_cannabis_by_U.S._jurisdiction`, but storage generation now resolves and merges a second-layer `Cannabis_in_*` page for all 50 states, including disambiguated titles like `Cannabis_in_Georgia_(U.S._state)` and `Cannabis_in_Washington_(state)`. Every generated `us-*` page now has `sources.legal`, `sources.wiki_truth = Legality_of_cannabis_by_U.S._jurisdiction`, and leading citations pointing at its own `Cannabis_in_*` article, while country records such as `geo` stay on `sources.wiki_truth = Legality_of_cannabis` plus country-specific `Cannabis_in_*` pages like `Cannabis_in_Georgia_(country)`. A live Playwright headed-pass now confirms the big-panel `Legal source →` opens the correct wiki article for `us-ca`, `us-tx`, `us-fl`, `us-ny`, `us-wa`, `us-ga`, and `geo` without state/country crossover. `Details →` from the small popup now keeps the same live MapLibre instance, updates the browser URL with `history.pushState`, re-centers the existing map, and opens the large SEO panel without a map remount on those same 6-country and 6-state interactive flows. US state coordinate ownership is now fixed too: when `data/centroids/us_adm1.json` lacks a state, storage generation falls back to `apps/web/public/us-states.geojson` label anchors instead of inheriting the USA center, which closes the `US-GA -> 38,-97` regression and keeps state `Details →` flows targetable. The local basemap tile proxy now fails soft: `/api/new-map/basemap-tile/[z]/[x]/[y]` returns an empty protobuf tile with `200 application/x-protobuf` on upstream fetch errors instead of surfacing a `500` AJAX overlay in the browser. Public favicon assets (`favicon.ico`, `favicon-master.png`, `icon-192.png`, `icon-512.png`, `apple-touch-icon.png`) are restored from the original local icon set instead of a generated recolor pass. Priority multilingual SEO is now storage-backed for a limited rollout: `data/wiki/cache/seo_i18n_priority.json` is generated only from Wikipedia `langlinks + summary` for `es/pt/de/fr/nl`, `[lang]/c/[code]` still pre-renders only real translated pairs, sitemap/hreflang publication stays availability-driven by that cache, and browser-language routing now uses the same cache truth. Production-mode validation on a separate local `next start --port 3101` confirms `/c/deu + Accept-Language: de` redirects to `/de/c/deu`, `/c/tha + Accept-Language: de` stays on EN, `/de/c/deu` renders `html lang=\"de\"` with self-canonical `/de/c/deu`, and `/de/c/tha` stays accessible but falls back to EN content with `html lang=\"en\"` and canonical `/c/tha`. AI companion runtime is now on a single context-driven generator path: `buildContext()` still reuses existing SSOT/social/airport/culture data, but `generateAnswer()` now dispatches to `generateLegal / generateTravel / generateCulture / generateGeneral`, the prompt is one explicit companion-core system prompt, short follow-ups like `а еще?` continue the prior topic via `dialogState.lastIntent` and `lastAnswer`, and anti-repeat only restructures non-follow-up answers instead of rebuilding the stack with more template blocks. V5 now adds a lightweight personality layer and few-shot style engine without new services: the system prompt explicitly sets calm/observant/grounded/slightly street-aware behavior, `fewShotDialogs.ts` seeds five dialogue examples into the prompt, and continuation now triggers on short follow-ups under 10 characters so the assistant keeps adding a new angle instead of restarting. AI companion runtime is now on a single context-driven generator path: `buildContext()` still reuses existing SSOT/social/airport/culture data, but `generateAnswer()` now dispatches to `generateLegal / generateTravel / generateCulture / generateGeneral`, short follow-ups reuse `dialogState.lastIntent` through `continueLastTopic()`, anti-repeat only reorders non-follow-up answers, and the local prompt is now one explicit companion-core system prompt instead of a thin template wrapper. The new nearby truth layer is live locally: `apps/web/src/core/truthModel.ts` computes `truthScore`/`truthClass` from existing law, enforcement, social, and access signals; `apps/web/src/lib/geo/nearbyTruth.ts` ranks countries and states by `distance * (1 / truthScore)` with `haversine`, adjacency-aware fallback, risk blocks, and a mandatory border warning; `/api/nearby` now returns this truth-ranked output; and AI nearby questions now reuse the same truth engine instead of binary status rules. Nearby intent drift is closed too: `buildContext()` preserves `nearby` across short follow-ups, `answerWithAssistant()` and the streaming API force `intent=\"nearby\"` whenever the nearby engine is selected, and `generateAnswer()` no longer rewrites nearby responses into generic legal prose during anti-repeat fallback. AI now has a dedicated provider layer in `src/ai-assistant/provider.ts`: provider selection is auto-resolved instead of hardwiring `production -> openai`, Ollama and OpenAI-compatible generation share one `generateWithProvider()` interface, warm/health checks use the same provider SSOT, local model selection still reorders by `data/ai/model_metrics.json`, and the route UI no longer disables input just because `NODE_ENV=production` if a real provider health-check succeeds. The nearby ranking now uses a cleaner pre-filter window instead of a post-sort guard: candidates are filtered by nearest-access eligibility rather than threshold alone, sorted first by `effective_distance_km`, reduced to the nearest `WINDOW_SIZE=8`, and only then reranked with distance as the primary signal whenever candidates differ by more than `100km`; truth-aware `effective_distance / (truth + boost + 0.05)` now decides only among genuinely close candidates. Inclusion inside nearby no longer drops “illegal but limited/tolerated in practice” regimes either: a country stays eligible through `truthScore >= 0.3` or through concrete access signals already present in SSOT/social data such as `possession_limit`, `rare|unenforced` enforcement, tolerated practice, or low-enforcement social flags, which keeps Iran-type and other fine-only restricted jurisdictions in the product-nearest window without reintroducing post-sort guards. AI dialogue context is now compressed and location-locked too: `buildContext()` resolves primary jurisdiction by `query explicit > locked dialog location > current UI/geo hint`, compare prompts keep the locked country as primary and only attach the mentioned compare country, `buildMessages()` now sends `system + compact summary + last assistant + last user + current user` instead of broad history, and non-nearby retries now hard-lock the answer back to the selected country or selected comparison pair when the model drifts. The AI UX layer now also has prompt v2 + dynamic few-shot: `fewShotDialogs.ts` selects short examples by intent (`legal`, `airport`, `culture`, `general`), `buildMessages()` injects those examples before the active user turn, system instructions explicitly ban generic filler, `generateAnswer()` rewrites near-duplicate replies locally instead of doing another LLM roundtrip, and too-short outputs are expanded into a non-empty grounded reply instead of returning a tiny stub.
Now: Local dev keeps the debug/runtime cards, truthful build watcher, full AI bar, Antarctica ASCII overlay, anchored infographic country popups on `/new-map`, and one shared map pipeline for `/`, `/new-map`, `/c/[code]`, and `/[lang]/c/[code]`. A direct Ollama live 6x6 dialog check on 2026-04-15 is now CONFIRMED for six installed local models on the Germany/Netherlands thread: `qwen2.5:1.5b=6/6`, `llama3.2:3b=6/6`, `smollm2:1.7b=6/6`, `phi3:mini=4/6`, `phi4-mini:3.8b=0/6`, `qwen3:4b=0/6`, with the slow two failing every turn by `TIMEOUT` at `25s`. The AI companion transport path is now partially fixed too: interactive defaults are trimmed to `qwen2.5:1.5b` and `llama3.2:3b`, exact `model` override now bypasses `working_models` cache in provider health resolution, local loopback/unknown requests no longer hit the shared rate-limit bucket, `buildMessages()` now stays on the minimal live context shape (`system + summary + last assistant + last user + current user`), and `AIBar` now preserves partial text on abort, batches chunk appends at `50ms`, parses the final NDJSON buffer on stream close instead of dropping the last payload, and now also parses a trailing unterminated NDJSON event on stream completion. The route now also short-circuits `x-ai-reset=1` without spending a live model turn, and the Ollama first-token timeout is raised to `8s` for real generation. Provider stream success is stricter too: `runOllamaChat()` now tracks token presence explicitly and throws `NO_TOKENS` instead of treating an empty stream as a usable success, and `generateWithProvider()` now does one warm+retry pass on transient Ollama errors before marking the model as failed. Direct live route verification on `127.0.0.1:3000` now confirms `GET /api/ai-assistant/query?model=llama3.2:3b` returns `model=llama3.2:3b` even while `working_models=["qwen2.5:1.5b"]`, and direct streamed `POST /api/ai-assistant/query` returns real multi-chunk text for `Hi`. Browser-side WebKit verification is improved too: a manual Playwright page session on the same localhost now gets a real first-turn assistant answer from `AIBar` instead of immediate `Request failed.`. The live Playwright proof was expanded to a real `6 countries × 6 questions` spec using only `qwen2.5:1.5b` and `llama3.2:3b`, and the harness now binds scenario geos through persisted browser location state before each country thread. A live Playwright-Interactive browser harness through `js_repl` now also CONFIRMS `qwen2.5:1.5b` passes the full `6×6` country suite on the current localhost outside the WebKit runner bottleneck: `Germany`, `Netherlands`, `Spain`, `Portugal`, `India`, and `Thailand` each completed `6/6` turns in the browser with answer length `>80` and without `Request failed.` or empty assistant bubbles. The local AI assistant now behaves like a grounded companion instead of a block assembler: the same `buildContext()` feeds one generator path by intent, short follow-ups continue the previous thread, dialog memory stores `lastAnswer` only to avoid near-duplicate replies without introducing any new service or state layer, and the prompt carries five few-shot dialogue examples plus a minimal personality contract. The route-side map/SEO behavior remains locked: `/c/[code]` shows the large SEO panel only for the route geo, ordinary country clicks keep the small anchored status popup path, route `i` markers survive client-side `Details →` transitions, and state routes like `/c/us-ga` keep the correct geo instead of drifting to the USA center. The local AI path now also has persisted model metrics: `tools/ai/evaluateModels.ts` runs real route dialogues against the live product, saves `data/ai/model_metrics.json`, and runtime reorders the local model chain from those scores. Nearby truth answers are now stable in both API and UI for Germany, Iran, and Thailand on 4-question flows (`where can I smoke near me` / `tolerated` / `borders` / `safer`): each reply stays on truth-engine, includes km distance plus the border-warning, and no longer falls back into generic legal prose on later turns. The nearby distance layer itself is now fully product-locked as `distance = access`: results still keep raw `distance_km`, but each result now also emits structured `distance.{raw_km,effective_km,type}` and ranking still uses `effective_distance_km`, `border_entry` keeps its 0.8 factor, same-country/direct stays 1.0, island-style destinations take a 1.2 penalty, and access modifies effective distance too (`legal 0.9`, `mostly_allowed 1.0`, `tolerated 1.05`, `limited 1.1`, `strict` excluded by threshold). The final nearby slice now stays simpler and more user-sense-correct: nearest candidates are chosen by the `k=8` window, distance decides whenever the gap inside that window is larger than `100km`, and truth only breaks ties among genuinely close options; restricted fine-only or tolerated-in-practice regimes still stay eligible through concrete access signals instead of dropping out at the threshold edge. Germany selected-country fallback now resolves neighbor-first through a merged adjacency layer instead of sparse raw graph data, `/api/nearby` tests enforce that NL outranks ES, and Iran now has a product floor of `truthScore >= 0.35` for restricted fine-only regimes so it remains included instead of collapsing to strict-only emptiness. The WHERE_IS_LEGAL_NEAR final lock is now applied minimally too: ranking keeps distance primary, removes the extra truth/access boost from close-range sorting, preserves `WINDOW_SIZE = 8`, preserves inclusion for `truthScore >= 0.3` or concrete `possession_limit` / `low_enforcement` / `tolerated` signals, and keeps the fixed warning `Crossing borders with cannabis is illegal in most countries.`. The nearby semantic path now also classifies `куда поехать рядом` as nearby intent, and targeted tests confirm Germany neighbor-first ordering, Iran inclusion, nearby warning stability, and nearby-intent routing. Provider auto-resolution now prefers a real available backend over a fake production default: on local production-mode starts with `AI_ENABLE_PROD=1` and no OpenAI key, the app uses Ollama instead of returning `External AI provider is not configured`, and route health/warm checks unlock the AI input when that provider is actually reachable. Ollama stream cleanup is now stricter too: idle stream breaks cancel the reader, model unload has its own timeout, refusal/disclaimer outputs such as `I'm sorry, but I can't assist with that` or `Not legal advice.` are treated as unusable model results instead of successful companion answers, and the provider now parses the final unterminated NDJSON line so the last Ollama chunk is not silently lost. AI model selection is now hard-filtered by real health instead of installed tags: `modelHealth.ts` persists `data/ai/working_models.json`, health probes require a tokenized non-refusal hello within 5s, runtime records per-model success/fail plus latency/length stats, and the live `GET /api/ai-assistant/query` on `127.0.0.1:3000` now reports `working_models=["qwen2.5:1.5b"]` while excluding slower or silent installed tags from the active chain. Query context on `/new-map` is also more honest now: if the user explicitly mentions a jurisdiction such as Germany while ambient `geo_hint` points to another place like Norway, runtime uses the mentioned jurisdiction as primary legal context instead of leaking the background geo into the answer. AI answers now also pass through one ultra-light dialog style post-processor only after generation: empty or sub-20-char replies are replaced with a humanized non-empty fallback, normal answers get a deterministic opener plus an intent-aware follow-up CTA, and light wording softening is applied without touching provider/context/API paths or mutating the mandatory nearby border warning.
Open questions: AI-companion production readiness is still CONFIRMED as not achieved locally. The 2026-04-15 direct Ollama 6x6 dialog check proves that installed-model transport is broader than the app-route cache currently exposes: `qwen2.5:1.5b`, `llama3.2:3b`, and `smollm2:1.7b` each completed `6/6` turns, `phi3:mini` completed `4/6`, and `phi4-mini:3.8b` plus `qwen3:4b` timed out on all `6/6` turns at `25s`. The live route now does honor exact `model` override and no longer rate-limits loopback, and the browser-side UI no longer fails trivially on a broken stream parser. Runner-level WebKit Playwright remains UNCONFIRMED on the current localhost because the classic `playwright test` path still showed late-turn instability earlier, but the same `6 countries × 6 questions` flow is now CONFIRMED through the live Playwright-Interactive `js_repl` browser harness for `qwen2.5:1.5b`. Contextual quality is also still unstable inside longer legal dialogues: direct Ollama answers from `smollm2:1.7b` and `phi3:mini` showed factual drift despite non-empty output, and the browser-passing `qwen2.5:1.5b` answers are still verbose and sometimes weak on factual nuance even when the turn technically succeeds. A new headed culture/history UI matrix on `2026-04-16` over `5 countries × 6 off-topic questions` now proves the same split on localhost: `qwen2.5:1.5b` stayed live on `29/30` turns with `21/30` prompt-hits but still showed `6` mixed-script artifacts, while `deepseek-coder:1.3b` stayed live on `30/30` turns yet produced `20/30` fallback-like disclaimers and only `8/30` usable passes. Prompt context no longer drags the previous subtopic into a new non-follow-up question after the latest `prompt.ts` fix; the targeted rerun removed the earlier `420`-bleed across `films/love-not-war/history`, but answer quality for those off-topic culture prompts remains weak and model-limited. `qwen3:4b` and `phi4-mini:3.8b` remain installed locally and confirmed in `ollama list`, while exact-tag `smollm3:3b` is unavailable in this environment and no community substitute is currently installed. `gemma:2b` remains UNCONFIRMED in this turn because it was not included in the direct 6x6 batch. UNCONFIRMED strict `ΔRGB ≤ 2` on the Germany sample only; water, AU, RU, and India matched reference values directly. UNCONFIRMED automated pixel-readback visibility for the delayed-start Antarctica ASCII path in Chromium (`asciiState="running"` but `nonEmptyPixels=0` in the automated probe). UNCONFIRMED local/wiki-only prison evidence for live `TT`: generic parser supports prison-bearing traversal docs, but the current cached Wikipedia lead/main-article extract for Trinidad and Tobago does not itself contain a prison term. UNCONFIRMED hot-reloaded user dev server behavior on `127.0.0.1:3000` for the multilingual middleware/layout path remains as before under the UI singleton rule.
