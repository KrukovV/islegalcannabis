Goal: Keep the product on a clean mapless baseline while a brand-new isolated `/new-map` skeleton grows separately from `/wiki-truth` and legacy map code.
State: checkpoint=.checkpoints/20260406-123918.patch; CI=PASS; Smoke=PASS
Done: Active product home UI still mounts only `MapRemovedScreen`; no interactive map runtime, map routes, hover pipeline, or map-specific CI stages returned to the product path. `tools/audit/no_map_imports.mjs` still reports `MAP_RUNTIME_REMOVED=1`, `MAP_IMPORTS_FOUND=0`, `MAP_ROUTES_REMOVED=1` for the legacy runtime. `/wiki-truth` live probe still passes with `WIKI_TRUTH_LIVE_OK=1`, `WIKI_TRUTH_ROW_COUNT=201`, and `WIKI_TRUTH_RUNTIME_PARITY=1`. `tools/pass_cycle.sh` remains locked to the mapless baseline and asserts `LEGACY_MAP_STAGE_COUNT=0` and `PASS_CYCLE_MAPLESS_ASSERT=PASS`; latest confirmed `bash tools/pass_cycle.sh` is green with `CI_STATUS=PASS`, `FAIL_REASON=NONE`, `SMOKE_STATUS=PASS`, `SMOKE_TOTAL=8`, `SMOKE_PASSED=8`, `SMOKE_FAILED=0`, `POST_CHECKS_OK=1`, and `HUB_STAGE_REPORT_OK=1`. The isolated `/new-map` skeleton uses MapLibre as the only render owner with native world copies, stronger atlas-matched legality palette (`ILLEGAL=#e7adb2`, `LEGAL=#9cd8a8`, `MIXED=#f2dc8f`, `UNKNOWN=#c8d1da`, base opacity `0.50–0.56`), strict flat-2D camera ownership, feature-state hover, and dateline-safe country preprocessing. The base/hover tint contract stays inverted so the atlas remains denser by default while hovered countries become lighter (`hoverColor` brighten `0.24`, `hoverOpacity = fillOpacity - 0.12`). Country/ocean/sea readability is handled only through native style paint/layout tuning: darker text, stronger white halo, smoother zoom expressions, and no forced overlap. `Caspian Sea` is back on the native sea-label path and now renders again in live Chromium at mid zoom. `Black Sea` still needs one minimal supplemental sea-label point source because the current CARTO basemap source does not expose that label natively in the tested cameras, but its zoom contract now matches the native sea-label path and it stays visible in live Chromium at zoom `3.9 / 5.4 / 7.0`. `/new-map` runtime card now owns snapshot actuality through `RuntimeParityBadge` (`Actual` green / `No Actual` red), and the bottom-right build update banner is suppressed on `/new-map` so that page has a single freshness owner. `RuntimeParityBadge` no longer hydrates from `window` during the first render; SSR and first client markup now both start from the same pending state, which removes the `/new-map` hydration mismatch. A separate root-level hydration warning from browser extensions is now suppressed at the framework boundary by `suppressHydrationWarning` on `<html>`, so attributes injected by tools like Tag Assistant no longer raise a false app-level mismatch overlay. Historical oversized screenshots/diagnostics were moved to `~/islegalcannabis_archive/repo_storage_cleanup_20260328/`, restoring `LOG_SIZE_GUARD=PASS` without reintroducing legacy map code. `/new-map` basemap now goes through same-origin routes (`/api/new-map/basemap-style`, `/api/new-map/basemap-source`, `/api/new-map/basemap-tile/{z}/{x}/{y}`) with browser-facing absolute URLs derived only from request host/proto and fallbacking to `http://127.0.0.1:3000`; live `/api/new-map/basemap-style` now returns `sources.carto.url=http://127.0.0.1:3000/api/new-map/basemap-source`, live `/api/new-map/basemap-source` returns `tiles=http://127.0.0.1:3000/api/new-map/basemap-tile/{z}/{x}/{y}`, and live Playwright WebKit on `http://127.0.0.1:3000/new-map` confirms `.maplibregl-canvas=1`, no invalid-URL overlay, `RuntimeParityBadge=Actual`, and no console/page errors. Country labels are bilingual again, but only country layers were changed: `place_country_*` now render `English + local name`, while seas, oceans, capitals, and small cities remain on their native paths. `/new-map` keeps its SSOT-only `/api/ai/query` path unchanged. A second isolated assistant runtime now lives under `apps/web/src/ai-assistant` and `data/ai/*`, uses only local legal/culture JSON plus cosine-style lexical retrieval, calls Ollama `llama3` opportunistically, falls back to safe general answers when retrieval is thin, injects travel-risk warnings for airport/border/carry questions, keeps only a tiny last-topic dialog memory, and leaves MapLibre, hover, and SSOT untouched. The AI assistant now also owns a static global airports dataset built from OurAirports (`data/ai/travel/airports.json`), groups airport lookups by ISO country and US state, resolves travel geo from the query itself, appends SSOT-backed travel risk from the same `/api/check`-compatible law path, and adds jurisdiction context with normalized notes plus source titles so travel/legal answers no longer leak unrelated countries. The same local assistant now resolves all `249` ISO countries plus `51` US state geos through `resolveLocation`, accepts `/api/check?country=US-CA` / `US-TX`, uses `data/ai/countries_aliases.json`, keeps culture data in `data/ai/*` without destructive rewrites, persists chat history across close/reopen/reload, and now renders legal dialogue in the order `Normalized notes -> Social reality -> final legal summary -> practical risk -> official corroboration`. Local structured AI data now also includes `data/ai/culture/*` topic files and `data/ai/travel/travel_risks.json`, while `Reports/ai-coverage.json` is clean at `ok=300`, `missing=0`, `errors=0`, `fallbackOnlyStates=0`, and `airportGeoMismatches=0`. `AIBar` now points to `/api/ai-assistant/query` with only `message` and `geo_hint`, keeps answers scrollable at `40vh`, and live Playwright Chromium on `/new-map` confirms working answers for Bob Marley birth (`1945`), follow-up `what else?`, airport travel risk, reggae colors (`red/gold/green`), and unknown-query fallback. The chat input is now closed in production on both layers: the `/new-map` UI keeps only the GPS button interactive while `/api/ai-assistant/query` rejects production requests with `AI_DISABLED`, so prod users cannot send chat prompts even if they bypass the disabled input. API geo is now strict UI-owned input only: query text no longer auto-detects country outside the assistant travel resolver, missing geo returns `Unknown. No country context provided.`, GPS status stays decoupled from AI, and one global `__MAP_SELECTED_GEO__` callback from the fill layer restores click → country card and makes the AI bar placeholder/context follow selected country only. Feature-state hover ownership is restored after the UI geo fix by reattaching `attachHoverController(runtime.map)` in `MapRoot`, so hover, click card, and AI placeholder now coexist without changing render/style/feature-state logic; the same controller now also drives `us-states-fill`, so zoomed-in US states use the same native hover behavior as countries instead of staying static. The published `us-states.geojson` static asset is enriched again on both localhost and production (`1d5c88512aea323f795370a50101e6d29a2f01f6`): `US-CA/US-TX/US-NY` now expose `geo`, `displayName`, `legalColor`, `hoverColor`, `fillOpacity`, and `hoverOpacity`, and direct Playwright WebKit probes confirm `hoveredId=US-CA` plus working anchored California popup on both `http://127.0.0.1:3000/new-map` and `https://islegal.info/new-map`. `/new-map` current-location bootstrap no longer owns first paint: `MapGeoDock` now lazy-loads the geo hook and AI dock after the map shell starts, while `MapRoot` keeps only map construction, style/countries fetch reuse, and popup rendering on the first chunk. Geo persistence remains a single-object owner path: `localStorage[\"geo\"]` stores only one `{ lat, lng, source, iso2? }`, restored on mount; GPS stays manual-only, immediately becomes the current location owner on permission grant, survives reloads, and cannot be downgraded by later IP refreshes. Saved IP coordinates are reused when GPS is absent, then refreshed in the background; clicking the green `GPS` still recenters the map back to the current resolved location without touching hover, AI ownership, or MapLibre layers. Persisted geo now reapplies directly to the live MapLibre instance after map mount: restored GPS/IP coordinates recreate the user marker and recenter the map on init without waiting for a new live geo event, and the restored-marker smoke on a fresh prod-like build passes. Browser smoke is now SSOT-driven through `Reports/smoke-report.json`; `apps/web/playwright.config.ts` always emits a JSON report, `tools/playwright-smoke/normalize_report.mjs` normalizes it, `tools/ci-local.sh` and `tools/pass_cycle.sh` now require `total>=5` with zero failures, `tools/commit_if_green.sh` blocks publish unless `SMOKE_STATUS=PASS`, and the smoke target is the actual isolated smoke server via `PLAYWRIGHT_BASE_URL` rather than a stale `:3000` default. Published `origin/main` now also includes `a73e1c3dac924adeeb2326b4e9be5b8897c93ef4` (`good/20260405-new-map-startup-trace`) and `ea899a5ee426f78b210d8ae1335d524bc1f73c11` (`good/20260405-new-map-startup-memo`). `/new-map` startup now has a single first-visual owner in the map runtime: the page emits `NM_T0..T11` marks into `window.__NEW_MAP_TRACE__`, defers `RuntimeMiddleware`, `BuildWatcher`, `GeoInit`, and the `RuntimeParityBadge` fetch until after first fill on the `/new-map` route, and records local/prod Chromium/WebKit artifacts under `Reports/new-map-startup.*.json` plus `Reports/new-map-startup.delta.md`. `/new-map` itself is now emitted as a static app route (`○ /new-map`) instead of a request-time dynamic page, so the server no longer blocks each page load on `headers()`/runtime-identity work before MapLibre can boot. The primary document payload on `/new-map` is now also slimmed: popup card data and the US states GeoJSON no longer ride inside the HTML/RSC response, because they are served from static assets (`/new-map-card-index.json`, `/us-states.geojson`) instead. That shrank the isolated prod-like HTML response to ~11 KB, and the latest isolated trace on `http://127.0.0.1:4010/new-map` now shows `requestStartMs≈21/50` and `NM_T2_MAP_CONSTRUCTOR_START≈275/318` in Chromium/WebKit respectively. Live production still has a parity gap, but the same trace improved to roughly `requestStartMs≈1522/1700` and `NM_T2≈1583/684`, confirming the remaining bottleneck sits in prod delivery before first map execution rather than in the map code itself. `node tools/compare_snapshot.mjs` reports `mismatches=0` against `https://islegal.info/api/new-map/countries`, so prod map statuses are 1:1 with the vendored local snapshot. Local `/wiki-truth` now renders three separate note columns in one table: raw `Wiki notes`, real normalized notes from `claim.notes_text`, and a preserved `Notes explainability` summary from the resolver layer; the truth-page test expectation now matches that schema and the truth test stage is green again. Local baseline remains published on `origin/main`; `map-stable-v1` continues to mark the prior stable checkpoint. The web app now serves favicon assets only from `apps/web/public/`: a generated multi-size `favicon.ico` (`16/32/48`) plus `favicon-16x16.png`, `favicon-32x32.png`, `favicon-512.png`, `web-app-manifest-192.png`, and `apple-touch-icon.png`, all rebuilt from the white-leaf black-circle source through `scripts/generate-favicon.py`; `apps/web/src/app/favicon.ico`, `public/favicon.png`, and `public/apple-touch-icon-precomposed.png` are removed so runtime ownership is public-only with no app/public conflict. `apps/web/src/app/layout.tsx` now declares `icon=/favicon.ico`, `shortcut=/favicon.ico`, explicit `32x32/16x16` PNG fallbacks, and `apple=/apple-touch-icon.png` without changing layout structure or runtime behavior. `@vercel/analytics` is mounted in `apps/web/src/app/layout.tsx` after `{children}` and is confirmed both locally and on production as the Vercel analytics script with `data-sdkn=\"@vercel/analytics/next\"`. `/new-map` now starts style/countries from a raw inline `<head>` prefetch script and reuses the same style promise during map boot, so the previous `await stylePromise -> createMap()` waterfall is removed. On the latest isolated smoke run at `http://127.0.0.1:4010/new-map`, popup smoke passes across mainland and island countries (`FR`, `JP`, `IS`), zoom-based USA decomposition is live, and `US-CA` opens through the same anchored popup pipeline without regressing GPS restore or the early-request preload smoke. Local dev runtime metadata now marks an uncommitted worktree as `COMMIT=<head>-dirty`, so restarting `npm run dev` on `:3000` shows the current development tree even before a git commit. `/new-map` now also owns a native background fallback for tile gaps: the empty bootstrap style includes a bottom `background` layer, the live style is repaired to keep that layer at the bottom if a basemap style omits it, and the map surface/canvas pair now renders `#c6d0d7` instead of white while tiles catch up during fast zoom-out or pan.
Now: New map skeleton exists only on `/new-map`; home and `pass_cycle` stay mapless until an explicit cutover.
Open questions: UNCONFIRMED post-proxy `/new-map` smoke: latest `tools/new_map_cycle.sh` is red with `NEW_MAP_RUNTIME_OK=0`, `NEW_MAP_LABELS_OK=0`, `NEW_MAP_SEA_LABELS_OK=0`, `NEW_MAP_HORIZONTAL_PAN_OK=0`, `NEW_MAP_HOVER_STALE=1`, and `LABELS_LOST_AFTER_PAN=2`; the live browser defect is fixed, but the headless smoke still needs re-baselining to the proxied basemap contract.
