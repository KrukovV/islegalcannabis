Goal: Keep one canonical `/new-map` MapLibre runtime across `/`, `/new-map`, and `/c/[code]`, with CountryPageData as the only SEO/map/card truth, stable nearby/social-reality enrichment, exact non-regressing map visuals, truthful local overlays, and correct live “Where am I” behavior on SEO routes.
State: checkpoint=.checkpoints/20260412-020442.patch; CI=PASS; Smoke=PASS
Done: CountryPageData now drives `/c/[code]` SSR, `/api/new-map/card-index`, `/api/new-map/countries`, and the SEO overlay panel with graph links/citations; nearby/social-reality is generated statically with `255` country-or-region entries; `/c/[code]` reuses the same map runtime and `i` marker/panel flow as `/new-map`; prod keeps analytics, favicon SSOT, hidden debug/update overlays, locked AI input, native admin boundaries, inland water above fills, visible Antarctica, and bundle-safe US state colors. Canonical SEO storage extends to `288` nodes with `50` `us-*` state pages, CountryPageData `v2` carries raw-vs-final medical plus `distribution`, the `rec_implies_med_floor` invariant leaves `0` rec/med conflicts in generated country pages, and `apps/web/src/lib/countryStatusDerivation.js` now runs a single priority-sorted `RULES_TABLE` with `applied_rules`. GitHub publish for the latest raw-first legality fix completed on `origin/main=17ddcdb1ce592898ebd95105179f71383cda6909`; `git ls-remote --heads origin main` confirms the same SHA, and Vercel production has now switched to the same build: `https://islegal.info/api/build-meta` reports `commit=17ddcdb1ce592898ebd95105179f71383cda6909` with `builtAt=2026-04-11T20:24:31.570Z`. Live SSR output now matches the fixed derived model on key routes: `/c/nld` serves `Cannabis is tolerated in Netherlands / Nederland.`, `/c/rus` serves `Cannabis is illegal in Russia / Россия.` with `Prison exposure detected.`, and `/c/chn` serves `Cannabis is illegal in China / 中国.`. The US-state isolation fix is now also published and live on production in `ee37871b7c45dc917a3061770feb052ac03072ca`; `https://islegal.info/api/build-meta` reports that commit with `builtAt=2026-04-11T20:36:08.828Z`. Live SSR for control routes now matches state-specific truth instead of country leakage: `/c/us-ca` is recreational `LEGAL`, `/c/us-tx` is recreational `ILLEGAL` with medical availability, `/c/us-id` is `ILLEGAL`, `/c/us-ny` is `LEGAL`, and `/c/us-fl` is recreational `ILLEGAL` with medical availability. Local dev `http://127.0.0.1:3000/api/build-meta` is now confirmed in this turn and reports `buildId=L4_4kQlewwCSWD4zU1_M0`, `commit=ee37871-dirty`, `builtAt=2026-04-11T15:23:45.037Z`, and `snapshotBuiltAt=2026-04-11T21:38:37.081Z`; the local `/new-map` SSR stamp now matches the same runtime values after the route was switched to dynamic SSR. Production DOM keeps the AI input locked and runtime/debug overlays hidden; local keeps the unlocked AI bar and visible runtime cards. The SEO route marker invariant is now explicit: one `/c/...` URL renders exactly one `i` marker for the route geo only. The previous “hedgehog” on `/c/usa` came from a `MapRoot` loop that instantiated markers for every `stateEntries` row in `seoCountryIndex`; that loop is removed. Local WebKit regression now passes for both `/c/us-ca` and `/c/usa` with exactly one `Open info for ...` marker on each route. The ASCII smoking actor now uses a six-state FSM (`idle -> lift -> near -> inhale -> exhale -> drop`) with a bent elbow/forearm silhouette, a short `=` joint, smoke embedded in-frame, and `walking-smoker` now does `walk -> hold -> smoke -> exit` instead of resetting a flat-arm loop. Deep wiki legal parsing now preserves traversal-derived penalties in CountryPageData via `legal_model.signals`: `countryStatusDerivation.js` is penalty-first, monotonic on `prison/arrest/fine`, keeps source URLs/confidence, and `/api/check` now returns `legal_status`, `penalties`, `confidence`, `sources`, and `explain` from the derived SSOT layer. Severity is now `max`-merged instead of additive, prison is OR-merged with `prison_priority` by source type (`summary=1`, `reference=2`, `traversal=3`), missing traversal cache now emits `no traversal evidence` instead of silently implying `no prison`, and risk is now separated from legal status: `signals.status` remains the legal/distribution layer while `signals.final_risk` is penalty-dominant and `penalties` are split by `possession` vs `trafficking`. BuildWatcher now escalates local update-banner refresh to a hard document navigation with a cache-busting query param and pending build stamp in `sessionStorage`, retries that navigation up to three times if the DOM stamp still mismatches, and compares `/api/build-meta` against the current DOM runtime stamp instead of a stale initial snapshot, so local `Доступно обновление` is tied to actual runtime mismatch only. A new route-config regression test now pins [page.config.test.ts](/Users/vitaliykryukov/Projects/islegalcannabis/apps/web/src/app/new-map/page.config.test.ts) to `export const dynamic = "force-dynamic";`, preventing the old `force-static` regression that made the local refresh banner impossible to satisfy. That route-freshness fix is now also published on production in `7147a8984e3c7715af2f8da7d43ed80d9f6780ca`; `https://islegal.info/api/build-meta` reports that commit with `builtAt=2026-04-11T21:47:42.316Z`, so GitHub and Vercel are aligned on the refreshable `/new-map` runtime. Storage generation now performs secondary-source enrichment in the same country pipeline: `tools/generate_country_page_storage.mjs` merges `data/wiki/wiki_traversal_cache.json` text with live Wikipedia API plaintext extracts for `notes_main_articles`, feeds that merged traversal text into the existing rule-engine without a second parser, and the latest regeneration completed with `COUNTRY_PAGE_STORAGE_OK countries=238 states=50 graph_nodes=288 graph_edges=2678`. Secondary-source semantics are now strict: traversal/reference input may strengthen penalties, scopes, and distribution, but country-level `recreational` and `medical` truth remains summary/table-first, so enriched main-article text no longer collapses `bel` or `nld` into false `ILLEGAL` nor upgrades Belgium medical to false `LEGAL`. Enforcement is now a first-class derived signal: `enforcement_level` is detected in the same pass (`active|rare|unenforced`), weak-enforcement phrases like `often unenforced` and `convictions are rare` now downgrade prison-driven outputs from total-red to `RESTRICTED`, regenerated `egy.json` now serializes `recreational.status=ILLEGAL`, `recreational.enforcement=MODERATE`, `signals.status=restricted`, `signals.final_risk=RESTRICTED`, and `signals.enforcement_level=unenforced`, while `rus.json` remains `illegal/HIGH_RISK` and `chn.json` remains `illegal` under active enforcement. `/api/check` now exposes `enforcement_level` in normal mode and adds `debug.has_article`, `debug.enforcement`, and `debug.penalties_detected` in `?debug=1`. Generator audit logging now prints `SECONDARY_SOURCE_AUDIT` for control geos (`EG/RU/CN/NL/TT`) plus `EMPTY_MAIN_ARTICLE` warnings when the Wikipedia API extract is short or empty. The raw-first legality fix is now live in generated SSOT too: `rus.json` and `chn.json` no longer serialize false `Cannabis is legal`, `recreational.status` for both is `ILLEGAL`, and weak reference title-hints no longer overwrite raw wiki illegality. The remaining Netherlands regression is closed: traversal sentences that say `illegal, but decriminalised/tolerated for personal use` no longer collapse recreational status to `ILLEGAL`, regenerated `nld.json` now serializes `recreational.status=TOLERATED`, `distribution.status=mixed`, and the full gate is green with `CI PASS`, `Smoke 8/0`, `POST_CHECKS_OK=1`, and `HUB_STAGE_REPORT_OK=1`. US state truth is now isolated from `Cannabis in the United States`: `buildStateEntries()` ignores `us_states_wiki.rec_status` and decodes state-specific law/raw sources only (`data/laws/us/*.json` first, then `{{Hs|...}}` from `legality_us_states.json`), raw row binding is fixed to wiki page titles instead of `name.slice(-2)`, and generated state coverage is now mixed rather than all-green: `23 LEGAL / 20 ILLEGAL / 7 DECRIMINALIZED`, with control states `CA=LEGAL`, `TX=ILLEGAL`, `ID=ILLEGAL`, `NY=LEGAL`, `FL=ILLEGAL+medical LEGAL`. Secondary-source processing is now deterministic at the signal layer instead of string-merging notes and article text into one input: summary docs and traversal/reference docs are parsed separately, article-derived penalties/enforcement/distribution are merged explicitly into the final signal set, `/api/check?debug=1` now exposes `has_article`, `article_len`, `source_confidence`, and notes-vs-article signal deltas, and storage generation now emits `NO_SECONDARY_SOURCE` / `SECONDARY_SOURCE_WEAK` coverage warnings instead of silently degrading. `/c/[code]` SSR now includes a real SEO article layer with five intent sections built only from `notes_normalized`, `legal_model`, and `facts`: buy, possession, tourists, airport, and medical. The ASCII overlay now treats SSOT frames as skeleton-only at render time: `ascii-engine.ts` strips ASCII joint/smoke glyphs from the body draw, computes a hand-anchored joint vector from the current mirrored frame, renders the joint plus red ember on canvas, and drives smoke through a fixed-size particle pool on the same canvas with RAF throttled to `<=30fps`.
Now: Local dev keeps the debug/runtime cards, truthful build watcher, full AI bar, Antarctica ASCII overlay, SEO overlay panel, and one shared map pipeline for `/`, `/new-map`, and `/c/[code]`. `/c/[code]` keeps the route-country panel/selection while the blue “Where am I” marker is owned only by live IP/GPS state, and the SEO `i` marker is now bound only to the route geo instead of all sibling states. The local ASCII runtime starts after `5s` in dev, auto-rotates through the registry after each scenario completes, and production keeps the long delayed-start behavior plus the hidden runtime/debug overlays and disabled AI input. Local SEO storage/index/graph include `us-*` nodes, state route pages use `geo_code`, and country truth carries raw-vs-final medical status plus single-pass rule-table `distribution` derivation for the map, popup, and `/api/check` consumers. The ASCII runtime now exposes the smoking FSM directly in `ascii-engine.ts`, smoking/pass geometry is sourced from `ascii-ssot.ts`, and the short-figure mirror invariant is covered by `ascii-ssot.test.ts` so future edits cannot reintroduce long-arm “fork” poses or ground-level smoke. Derived legal parsing now consumes cached traversal text plus live Wikipedia API plaintext extracts for canonical main-article titles during storage generation, while keeping summary/table truth authoritative for country-level recreational and medical statuses and softening only final risk/legal resolution when enforcement is weak. `/c/[code]` now server-renders the route summary card plus five intent H2 sections from storage truth, and the derived debug layer distinguishes `no_secondary_source` from `no_prison_signal` instead of treating both as parser uncertainty. Intent extraction is now deterministic from `notes_normalized`, `legal_model`, `facts`, and `signals`: `/c/[code]` sorts the five SSR H2 blocks by intent strength, ranks per-intent related regions by `intent_strength × geo_graph_weight`, supports optional server-side `?q=` intent boosting, and throws `COUNTRY_INTENT_INVALID` on legal-model contradictions instead of silently drifting. New-map status rendering is now single-source and server-computed: `CountryPageData -> deriveResultStatusFromCountryPageData() -> /api/new-map/countries -> map`, `CountryCardEntry` and GeoJSON features now carry `result.status` plus strict `statusToColor(result.status)`, `/api/check` exposes the same `result_status/result_color`, and the old pair/enforcement-based yellow uplift for `ILLEGAL` countries is removed. Countries without SSOT status are no longer painted via fallback; the server filters and logs them explicitly (`NEW_MAP_FILTERED_MISSING_STATUS`) instead of silently coloring them yellow.
Open questions: UNCONFIRMED strict `ΔRGB ≤ 2` on the Germany sample only; water, AU, RU, and India matched reference values directly. UNCONFIRMED automated pixel-readback visibility for the delayed-start Antarctica ASCII path in Chromium (`asciiState="running"` but `nonEmptyPixels=0` in the automated probe). UNCONFIRMED local/wiki-only prison evidence for live `TT`: generic parser supports prison-bearing traversal docs, but the current cached Wikipedia lead/main-article extract for Trinidad and Tobago does not itself contain a prison term.
