Goal: Keep one canonical `/new-map` MapLibre runtime across `/`, `/new-map`, and `/c/[code]`, with CountryPageData as the only SEO/map/card truth, stable nearby/social-reality enrichment, exact non-regressing map visuals, truthful local overlays, and correct live “Where am I” behavior on SEO routes.
State: checkpoint=.checkpoints/20260412-203258.patch; CI=PASS; Smoke=PASS
Done: CountryPageData now drives `/c/[code]` SSR, `/api/new-map/card-index`, `/api/new-map/countries`, and the SEO overlay panel with graph links/citations. The latest production build is `origin/main=638b2fe9046940fd5800f9e3d0c10590a839428d`, and `https://islegal.info/api/build-meta` reports the same commit with `builtAt=2026-04-12T15:56:23.188Z`. Popup/card output now carries `mapReason` for all non-trivial yellow and green buckets, the frozen pastel palette stays locked to exact HEX values, and timeline-aware current-law parsing now prefers the latest `isCurrent` law text so `DMA -> DECRIM`, `GEO -> DECRIM`, `ESP -> MIXED`, and `IRN -> ILLEGAL` stay stable. SEO is now split into one canonical country route plus alternate locale routes: `/sitemap.xml` is an XML sitemap index pointing at `/sitemap-main.xml`, `/sitemap-countries.xml`, `/sitemap-states.xml`, and `/sitemap-i18n.xml`; all sitemap entries now come from CountryPageData with truthful `updated_at -> lastmod` fallbacking only to build time; `/c/[code]` metadata now emits hreflang alternates for `en/es/fr/de`; and `/es|fr|de/c/[code]` SSR routes now exist with canonical back to `/c/[code]`, localized titles, and indexed hreflang sitemap alternates.
Now: Local dev keeps the debug/runtime cards, truthful build watcher, full AI bar, Antarctica ASCII overlay, SEO overlay panel, and one shared map pipeline for `/`, `/new-map`, `/c/[code]`, and `/es|fr|de/c/[code]`. `/c/[code]` keeps the route-country panel/selection while the blue “Where am I” marker is owned only by live IP/GPS state, and the SEO `i` marker is now bound only to the route geo instead of all sibling states. The local ASCII runtime starts after `5s` in dev, auto-rotates through the registry after each scenario completes, and production keeps the long delayed-start behavior plus the hidden runtime/debug overlays and disabled AI input. Local SEO storage/index/graph include `us-*` nodes, state route pages use `geo_code`, and country truth carries raw-vs-final medical status plus single-pass rule-table `distribution` derivation for the map, popup, and `/api/check` consumers. Derived legal parsing now consumes cached traversal text plus live Wikipedia API plaintext extracts for canonical main-article titles during storage generation, while keeping summary/table truth authoritative for country-level recreational and medical statuses and softening only final risk/legal resolution when enforcement is weak. `/c/[code]` now server-renders the route summary card plus five intent H2 sections from storage truth, and the derived debug layer distinguishes `no_secondary_source` from `no_prison_signal` instead of treating both as parser uncertainty. Intent extraction is now deterministic from `notes_normalized`, `legal_model`, `facts`, and `signals`: `/c/[code]` sorts the five SSR H2 blocks by intent strength, ranks per-intent related regions by `intent_strength × geo_graph_weight`, supports optional server-side `?q=` intent boosting, and throws `COUNTRY_INTENT_INVALID` on legal-model contradictions instead of silently drifting. New-map status rendering still keeps the explicit truth/view split and the frozen reference palette. `resultStatus.ts` now uses a single deterministic formula with final enum `LEGAL | MIXED | DECRIM | ILLEGAL | UNKNOWN`: baseline comes from wiki recreational table truth, softeners come only from personal-use/fine/tolerance signals in primary country text, strong negatives stay on death/long-prison/strict-enforcement, `USA` remains a deterministic `MIXED` branch, and map snapshot generation derives `mapCategory` from the explicit view-layer rules instead of a second truth path. Popup/card output now carries `mapReason` so yellow countries with formal illegality explicitly explain the yellow bucket instead of looking arbitrary, while colors remain locked to the exact pastel contract (`#cde7cf`, `#f4e9c2`, `#ead0d1`, `#d7dcdc` plus the fixed hover set). Countries without SSOT status are no longer painted via fallback; the server filters and logs them explicitly (`NEW_MAP_FILTERED_MISSING_STATUS`) instead of silently coloring them yellow. The `/c/[code]` top header overlay is now removed completely from SSR: the old `hero/panel` shell no longer renders above the map, while the single H1 and legal summary remain in normal DOM flow inside `#seo-content`. The local AI assistant is now a grounded companion instead of a thin template wrapper: `aiRuntime.ts` builds explicit context from `CountryPageData`, derived legal signals, notes, social reality, travel risk, culture chunks, and dialog memory, then generates the answer from that context or hands the same grounded context to the local model. Production still keeps the AI input disabled, but local dev now supports state geos like `US-CA` in the AI route and grounded airport/travel follow-ups without inventing facts. Local runtime refresh now uses one full runtime signature across badge and update-banner: `/new-map` SSR emits `runtimeMode/mapRuntime/expectedOrigin` on the hidden runtime node, `BuildWatcher` compares the same full signature as `RuntimeParityBadge`, and `Обновить` can now surface any real `No Actual` mismatch and converge the hidden/visible runtime stamp to `/api/build-meta` after the hard-reload path. Timeline-aware current-law parsing is now in the derived SSOT path too: `CountryPageData` exposes `legal_timeline[]`, `extractLegalTimeline()` pulls dated law/policy/enforcement snippets from notes/facts/signal titles/citations, and `deriveResultStatusFromCountryPageData()` now prefers the latest `isCurrent` text over mixed historical article noise when building flags. The current-law regressions are now pinned for `DMA -> DECRIM`, `GEO -> DECRIM`, `ESP -> MIXED`, and `IRN -> ILLEGAL`, while `BE` now settles at `DECRIM` under the stricter current-law modifier path instead of drifting back to `MIXED`. SEO now runs through a split XML sitemap/index contract from one SSOT: `/sitemap.xml` is the index, main/country/state/i18n sitemaps are generated from CountryPageData only, `lastmod` is `updated_at`-truth with build-time fallback only, and canonical `/c/*` pages now emit hreflang alternates to the locale routes.
Open questions: UNCONFIRMED strict `ΔRGB ≤ 2` on the Germany sample only; water, AU, RU, and India matched reference values directly. UNCONFIRMED automated pixel-readback visibility for the delayed-start Antarctica ASCII path in Chromium (`asciiState="running"` but `nonEmptyPixels=0` in the automated probe). UNCONFIRMED local/wiki-only prison evidence for live `TT`: generic parser supports prison-bearing traversal docs, but the current cached Wikipedia lead/main-article extract for Trinidad and Tobago does not itself contain a prison term.
