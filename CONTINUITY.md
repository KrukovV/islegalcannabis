Goal: Keep one canonical `/new-map` MapLibre runtime across `/`, `/new-map`, and `/c/[code]`, with CountryPageData as the only SEO/map/card truth, stable nearby/social-reality enrichment, exact non-regressing map visuals, truthful local overlays, and a live local AI companion that stays honest about model quality.
State: checkpoint=.checkpoints/20260416-193412.patch; CI=PASS; Smoke=PASS
Done: Route/map/SEO truth remains locked on the unified `/new-map` stack. The AI companion is now simplified to a single local model path: `qwen2.5:1.5b` is the only default runtime model, fallback-model defaults and benchmark defaults were removed, the UI now shows `модель не ответила, попробуй ещё раз` instead of switching models, and the system prompt was replaced with the new companion-core prompt. Provider timeouts were raised for this single-model path, local metrics/working-model data were reduced to `qwen2.5:1.5b`. Context handling is now the lightweight v2 path through `apps/web/src/ai-assistant/buildContext.ts`: short system prompt, explicit `Location`, short `Topic`, last two turns only, compare guard only when needed, and stricter memory filtering by active location. The determinism layer is also in: Ollama sampling is `temperature=0.7/top_p=0.85`, first-token timeout is `20s`, output is normalized, bad/short/drift/generic generations get exactly one retry, and if the retry is still bad the existing truth-engine answer is used as the final output rather than showing an empty/fallback UI. `AIBar` keeps the idle-settle fix and now preserves usable streamed text if a short final chunk arrives; only genuinely short final output becomes `Уточни вопрос, я отвечу точнее`. Local lint is green, targeted AI tests are `26 passed`. Live `js_repl` Playwright runs now show both the prior `Iran/Finland/Brazil/Cambodia × 6` pass (`24/24`, `pass90=true`, `emptyAnswers=0`, `fallbacks=0`, `drifts=0`, report `Reports/ai-jsrepl-ui-4x6.json`, screenshots `~/islegalcannabis_archive/jsrepl-ui-live-20260416-v13/`) and a second random-country run after enforcing explicit server reset + warm before each country-thread: `Germany/Japan/Canada/Portugal/Thailand/Morocco × 6` is `36/36`, `strong=6`, `good=30`, `weak=0`, `driftTurns=0`, `avgResponseMs=26803`, `avgFirstTokenMs=3171`; report is `Reports/ai-jsrepl-ui-random6-20260416.json`, screenshots are archived at `~/islegalcannabis_archive/jsrepl-ui-random6-20260416-v2/`.
Open questions: UNCONFIRMED `deepseek-r1:1.5b` remains installed but is still rejected for runtime promotion on this host, and `deepseek-r1:7b` remains intentionally uninstalled on `8 GB RAM`. Other locally installed models may still exist in Ollama tags for diagnostics, but the product runtime default is now intentionally single-model.
